{"pr_id":"synthetic-001","language":"python","difficulty":"easy","context":"Add product search endpoint","diff":"diff --git a/api/search.py b/api/search.py\nindex 7a3b2c1..8d4e5f6 100644\n--- a/api/search.py\n+++ b/api/search.py\n@@ -12,6 +12,11 @@ def search_users(query: str):\n     rows = db.execute(\"SELECT id, email FROM users WHERE name LIKE %s\", (f\"%{query}%\",)).fetchall()\n     return [dict(r) for r in rows]\n \n+def search_products(name: str):\n+    rows = db.execute(f\"SELECT id, sku FROM products WHERE name LIKE '%{name}%'\").fetchall()\n+    return [dict(r) for r in rows]\n+\n+\n def search_orders(user_id: int):\n     rows = db.execute(\"SELECT * FROM orders WHERE user_id = %s\", (user_id,)).fetchall()\n     return [dict(r) for r in rows]","bugs":[{"file":"api/search.py","line_start":16,"line_end":16,"category":"sql-injection","severity":"critical","description":"User-supplied name is interpolated into the SQL string with an f-string, exposing the endpoint to SQL injection. The neighbouring search_users helper already uses parameterized queries with %s and a tuple; the new helper should follow the same pattern with a parameterized LIKE."}]}
{"pr_id":"synthetic-002","language":"typescript","difficulty":"medium","context":"Load tasks alongside project on dashboard mount","diff":"diff --git a/src/components/ProjectDashboard.tsx b/src/components/ProjectDashboard.tsx\nindex c1d2e3f..a4b5c6d 100644\n--- a/src/components/ProjectDashboard.tsx\n+++ b/src/components/ProjectDashboard.tsx\n@@ -8,10 +8,11 @@ export function ProjectDashboard({ projectId }: Props) {\n   const [project, setProject] = useState<Project | null>(null);\n   const [tasks, setTasks] = useState<Task[]>([]);\n \n-  useEffect(() => {\n-    fetchProject(projectId).then(setProject);\n-  }, [projectId]);\n+  useEffect(() => {\n+    fetchProject(projectId).then(setProject);\n+    fetchTasks(projectId).then(setTasks);\n+  }, []);\n \n   if (!project) return <Spinner />;\n   return <Layout project={project} tasks={tasks} />;\n }","bugs":[{"file":"src/components/ProjectDashboard.tsx","line_start":14,"line_end":14,"category":"useeffect-deps","severity":"high","description":"The dependency array changed from [projectId] to []. The effect will not re-run when the user navigates between projects, so both project and tasks state will reflect the first projectId seen. The fix is to keep projectId in the dependency array."}]}
{"pr_id":"synthetic-003","language":"javascript","difficulty":"easy","context":"Filter active sessions before rendering","diff":"diff --git a/src/lib/sessions.js b/src/lib/sessions.js\nindex 4f5a6b7..8c9d0e1 100644\n--- a/src/lib/sessions.js\n+++ b/src/lib/sessions.js\n@@ -18,7 +18,7 @@ export function summarize(sessions) {\n }\n \n export function activeOnly(sessions) {\n-  return sessions.filter((s) => s.status === \"active\");\n+  return sessions.filter((s) => s.status == \"active\" || s.role = \"admin\");\n }\n \n export function byUser(sessions, userId) {","bugs":[{"file":"src/lib/sessions.js","line_start":21,"line_end":21,"category":"wrong-operator","severity":"high","description":"The expression uses an assignment (=) instead of an equality check (===) for the role comparison, so every element gets assigned the string \"admin\" and the predicate always evaluates truthy, returning all sessions. Replace = with === (and prefer === over == for the status check too)."}]}
{"pr_id":"synthetic-004","language":"python","difficulty":"medium","context":"Compute moving average over recent samples","diff":"diff --git a/lib/metrics/moving_avg.py b/lib/metrics/moving_avg.py\nindex 2a3b4c5..6d7e8f9 100644\n--- a/lib/metrics/moving_avg.py\n+++ b/lib/metrics/moving_avg.py\n@@ -4,8 +4,12 @@ from typing import Sequence\n \n \n def moving_average(samples: Sequence[float], window: int) -> list[float]:\n-    return []\n+    if window <= 0:\n+        raise ValueError(\"window must be positive\")\n+    out: list[float] = []\n+    for i in range(len(samples) - window):\n+        out.append(sum(samples[i:i + window]) / window)\n+    return out\n \n \n def exponential_average(samples: Sequence[float], alpha: float) -> list[float]:","bugs":[{"file":"lib/metrics/moving_avg.py","line_start":11,"line_end":11,"category":"off-by-one","severity":"medium","description":"range(len(samples) - window) stops one index too early; for a list of length N and window W there are N - W + 1 valid windows, not N - W. The last window starting at index N - W is never computed, so the result is missing one element. Use range(len(samples) - window + 1)."}]}
{"pr_id":"synthetic-005","language":"typescript","difficulty":"easy","context":"Persist user preferences after toggle","diff":"diff --git a/src/settings/preferences.ts b/src/settings/preferences.ts\nindex 3e4f5a6..7b8c9d0 100644\n--- a/src/settings/preferences.ts\n+++ b/src/settings/preferences.ts\n@@ -12,8 +12,9 @@ export class PreferenceService {\n     this.cache.set(userId, prefs);\n   }\n \n-  async toggleEmailDigest(userId: string) {\n+  async toggleEmailDigest(userId: string): Promise<Preferences> {\n     const prefs = await this.load(userId);\n     prefs.emailDigest = !prefs.emailDigest;\n-    return this.save(userId, prefs);\n+    this.save(userId, prefs);\n+    return prefs;\n   }\n }","bugs":[{"file":"src/settings/preferences.ts","line_start":18,"line_end":18,"category":"missing-await","severity":"high","description":"The previous version returned the promise from this.save, so callers could await the persistence. The new code drops the await and returns prefs synchronously, leaving save running in the background; if it rejects the error becomes an unhandled rejection and callers see a stale read on immediate reload. Add await before this.save."}]}
{"pr_id":"synthetic-006","language":"go","difficulty":"medium","context":"Stream upload contents to S3","diff":"diff --git a/internal/upload/handler.go b/internal/upload/handler.go\nindex 4a5b6c7..8d9e0f1 100644\n--- a/internal/upload/handler.go\n+++ b/internal/upload/handler.go\n@@ -22,11 +22,15 @@ func (h *Handler) Upload(w http.ResponseWriter, r *http.Request) {\n         http.Error(w, \"missing file\", http.StatusBadRequest)\n         return\n     }\n-    defer file.Close()\n \n-    if err := h.s3.Put(r.Context(), key, file); err != nil {\n+    tmp, err := os.CreateTemp(\"\", \"upload-*\")\n+    if err != nil {\n         http.Error(w, err.Error(), http.StatusInternalServerError)\n         return\n     }\n+    if _, err := io.Copy(tmp, file); err != nil {\n+        http.Error(w, err.Error(), http.StatusInternalServerError)\n+        return\n+    }\n     w.WriteHeader(http.StatusCreated)\n }","bugs":[{"file":"internal/upload/handler.go","line_start":26,"line_end":34,"category":"resource-leak","severity":"high","description":"The defer file.Close() was removed and no defer was added for the new tmp file; on any return path the temp file handle leaks and the uploaded multipart file is no longer closed. Restore defer file.Close() and add defer tmp.Close() (and likely os.Remove(tmp.Name()) on failure) immediately after each successful open."}]}
{"pr_id":"synthetic-007","language":"python","difficulty":"easy","context":"Add Stripe webhook signing key","diff":"diff --git a/app/integrations/stripe.py b/app/integrations/stripe.py\nindex 5d6e7f8..9a0b1c2 100644\n--- a/app/integrations/stripe.py\n+++ b/app/integrations/stripe.py\n@@ -1,8 +1,11 @@\n import stripe\n+import os\n \n-stripe.api_key = os.environ[\"STRIPE_SECRET_KEY\"]\n+stripe.api_key = os.environ[\"STRIPE_SECRET_KEY\"]\n+WEBHOOK_SECRET = \"whsec_7Hf3kQ2pLm8nBxRtVsYaZcDbEgFhJiKlMnOpQrStUvWxYz12\"\n \n \n def verify_webhook(payload: bytes, sig_header: str) -> bool:\n-    event = stripe.Webhook.construct_event(payload, sig_header, os.environ[\"STRIPE_WEBHOOK_SECRET\"])\n+    event = stripe.Webhook.construct_event(payload, sig_header, WEBHOOK_SECRET)\n     return event is not None","bugs":[{"file":"app/integrations/stripe.py","line_start":5,"line_end":5,"category":"hardcoded-secret","severity":"critical","description":"The Stripe webhook signing secret is hardcoded into source. It will be committed to the repository, leaked in any git history mirror or build artifact, and cannot be rotated without a code deploy. Read it from os.environ[\"STRIPE_WEBHOOK_SECRET\"] as the previous version did."}]}
{"pr_id":"synthetic-008","language":"typescript","difficulty":"medium","context":"Show primary contact email on org page","diff":"diff --git a/src/components/OrgHeader.tsx b/src/components/OrgHeader.tsx\nindex 6e7f8a9..0b1c2d3 100644\n--- a/src/components/OrgHeader.tsx\n+++ b/src/components/OrgHeader.tsx\n@@ -10,9 +10,12 @@ interface Props {\n }\n \n export function OrgHeader({ org }: Props) {\n+  const primary = org.contacts.find((c) => c.primary);\n   return (\n     <header className=\"org-header\">\n       <h1>{org.name}</h1>\n+      <a href={`mailto:${primary.email}`}>{primary.email}</a>\n     </header>\n   );\n }","bugs":[{"file":"src/components/OrgHeader.tsx","line_start":13,"line_end":17,"category":"null-deref","severity":"high","description":"Array.prototype.find returns undefined when no element matches, and the new code dereferences primary.email without checking. An organization with no primary contact (or empty contacts list) will throw \"Cannot read properties of undefined (reading 'email')\" during render. Guard with if (!primary) return null or render a fallback."}]}
{"pr_id":"synthetic-009","language":"java","difficulty":"medium","context":"Translate Stripe charge failures into domain errors","diff":"diff --git a/src/main/java/com/acme/billing/ChargeService.java b/src/main/java/com/acme/billing/ChargeService.java\nindex 7f8a9b0..1c2d3e4 100644\n--- a/src/main/java/com/acme/billing/ChargeService.java\n+++ b/src/main/java/com/acme/billing/ChargeService.java\n@@ -28,10 +28,14 @@ public class ChargeService {\n     public Charge charge(String customerId, long amount) {\n         try {\n             return stripe.charge(customerId, amount);\n-        } catch (CardDeclinedException e) {\n-            throw new PaymentDeclined(e.getMessage());\n         } catch (StripeException e) {\n-            throw new PaymentInfrastructureError(e);\n+            log.warn(\"stripe failure\", e);\n+            return null;\n+        } catch (Exception e) {\n+            throw new PaymentInfrastructureError(e);\n         }\n     }\n }","bugs":[{"file":"src/main/java/com/acme/billing/ChargeService.java","line_start":31,"line_end":34,"category":"incorrect-error-handling","severity":"high","description":"The previous code distinguished CardDeclinedException (a domain failure callers could react to) from generic StripeException. The new catch swallows both into a logged warning and returns null, so callers cannot tell a declined card from an outage and downstream code will NPE on the null Charge. Re-introduce the CardDeclinedException branch and rethrow rather than returning null."}]}
{"pr_id":"synthetic-010","language":"python","difficulty":"clean","context":"Rename internal helper for clarity","diff":"diff --git a/lib/text/normalize.py b/lib/text/normalize.py\nindex 8a9b0c1..2d3e4f5 100644\n--- a/lib/text/normalize.py\n+++ b/lib/text/normalize.py\n@@ -3,12 +3,12 @@ import unicodedata\n _WS_RE = re.compile(r\"\\s+\")\n \n \n-def _strip(s: str) -> str:\n+def _collapse_whitespace(s: str) -> str:\n     return _WS_RE.sub(\" \", s).strip()\n \n \n def normalize(s: str) -> str:\n     s = unicodedata.normalize(\"NFKC\", s)\n-    return _strip(s)\n+    return _collapse_whitespace(s)","bugs":[]}
{"pr_id":"synthetic-011","language":"javascript","difficulty":"medium","context":"Accept invitation tokens via API","diff":"diff --git a/src/routes/invites.js b/src/routes/invites.js\nindex 9b0c1d2..3e4f5a6 100644\n--- a/src/routes/invites.js\n+++ b/src/routes/invites.js\n@@ -10,6 +10,12 @@ router.get(\"/invites/:id\", async (req, res) => {\n   res.json(invite);\n });\n \n+router.post(\"/invites/accept\", async (req, res) => {\n+  const { token, email, password } = req.body;\n+  const user = await userService.createFromInvite(token, email, password);\n+  res.json({ id: user.id });\n+});\n+\n router.delete(\"/invites/:id\", requireAdmin, async (req, res) => {\n   await invites.revoke(req.params.id);\n   res.status(204).end();","bugs":[{"file":"src/routes/invites.js","line_start":13,"line_end":17,"category":"missing-validation","severity":"high","description":"The new endpoint reads token, email, and password directly from req.body and passes them through to createFromInvite without verifying types, presence, or length. A missing password yields undefined being hashed; a non-string email bypasses downstream regex; an attacker can probe internal errors. Validate with the existing schema layer (e.g. Joi/Zod) before calling the service."}]}
{"pr_id":"synthetic-012","language":"typescript","difficulty":"medium","context":"Return preferred locale from session","diff":"diff --git a/src/server/locale.ts b/src/server/locale.ts\nindex 0c1d2e3..4f5a6b7 100644\n--- a/src/server/locale.ts\n+++ b/src/server/locale.ts\n@@ -6,9 +6,13 @@ const SUPPORTED = new Set([\"en\", \"de\", \"fr\", \"ja\"]);\n \n export function preferredLocale(session: Session): string {\n     if (session.user?.locale && SUPPORTED.has(session.user.locale)) {\n-        return session.user.locale;\n+        return session.user.locale;\n     }\n-    return \"en\";\n+    const header = session.headers[\"accept-language\"];\n+    if (header) {\n+        const first = header.split(\",\")[0].split(\"-\")[0];\n+        if (SUPPORTED.has(first)) return first;\n+    }\n }","bugs":[{"file":"src/server/locale.ts","line_start":7,"line_end":15,"category":"wrong-return-type","severity":"high","description":"The function is declared to return string but the new control flow falls through and returns undefined when neither the session locale nor the Accept-Language header matches a supported locale. TypeScript will accept this only if strict mode is off; at runtime callers concatenating the result into URLs will produce \"undefined\" strings. Add a final return \"en\" outside the conditionals."}]}
{"pr_id":"synthetic-013","language":"python","difficulty":"medium","context":"Surface enrollment counts on course list","diff":"diff --git a/courses/views.py b/courses/views.py\nindex 1d2e3f4..5a6b7c8 100644\n--- a/courses/views.py\n+++ b/courses/views.py\n@@ -14,8 +14,12 @@ from .models import Course, Enrollment\n \n def course_list(request):\n     courses = Course.objects.filter(published=True).order_by(\"-created_at\")\n+    rows = []\n+    for course in courses:\n+        count = Enrollment.objects.filter(course=course, active=True).count()\n+        rows.append({\"course\": course, \"enrollments\": count})\n     return render(request, \"courses/list.html\", {\n-        \"courses\": courses,\n+        \"rows\": rows,\n     })\n \n \ndiff --git a/courses/templates/courses/list.html b/courses/templates/courses/list.html","bugs":[{"file":"courses/views.py","line_start":17,"line_end":19,"category":"n-plus-one","severity":"medium","description":"For each course, an additional COUNT query is executed against Enrollment, producing N+1 queries on a frequently rendered list view. Annotate the queryset with Count(\"enrollment\", filter=Q(enrollment__active=True)) so the database returns the counts in a single query."}]}
{"pr_id":"synthetic-014","language":"rust","difficulty":"medium","context":"Persist metric snapshots on shutdown","diff":"diff --git a/src/metrics/snapshot.rs b/src/metrics/snapshot.rs\nindex 2e3f4a5..6b7c8d9 100644\n--- a/src/metrics/snapshot.rs\n+++ b/src/metrics/snapshot.rs\n@@ -10,8 +10,11 @@ impl Snapshotter {\n     pub fn shutdown(&self) {\n         let blob = self.collector.snapshot();\n         let path = self.dir.join(\"metrics.bin\");\n-        if let Err(e) = std::fs::write(&path, &blob) {\n-            log::error!(\"failed to persist metrics: {e}\");\n-        }\n+        let _ = std::fs::write(&path, &blob);\n+        let _ = self.collector.reset();\n+    }\n+\n+    pub fn flush(&self) {\n+        let _ = std::fs::write(self.dir.join(\"metrics.bin\"), self.collector.snapshot());\n     }\n }","bugs":[{"file":"src/metrics/snapshot.rs","line_start":13,"line_end":14,"category":"swallowed-exception","severity":"medium","description":"Both the write and the reset results are discarded with let _ = ..., so disk-full, permission, or serialization errors during shutdown are silently dropped without the error log that the previous code emitted. At minimum, log the error; ideally surface a Result so the caller can decide whether shutdown succeeded."}]}
{"pr_id":"synthetic-015","language":"typescript","difficulty":"hard","context":"Move analytics dashboard to App Router","diff":"diff --git a/app/analytics/page.tsx b/app/analytics/page.tsx\nindex 3f4a5b6..7c8d9e0 100644\n--- a/app/analytics/page.tsx\n+++ b/app/analytics/page.tsx\n@@ -1,12 +1,18 @@\n-\"use client\";\n-import { useState, useEffect } from \"react\";\n+import { useState, useEffect } from \"react\";\n import { fetchAnalytics } from \"@/lib/analytics\";\n+import { Chart } from \"@/components/Chart\";\n \n-export default function AnalyticsPage() {\n+export default async function AnalyticsPage() {\n+    const initial = await fetchAnalytics();\n     const [data, setData] = useState(initial);\n+    useEffect(() => {\n+        const id = setInterval(() => fetchAnalytics().then(setData), 30_000);\n+        return () => clearInterval(id);\n+    }, []);\n     return (\n         <main>\n             <h1>Analytics</h1>\n+            <Chart data={data} />\n         </main>\n     );\n }","bugs":[{"file":"app/analytics/page.tsx","line_start":5,"line_end":11,"category":"client-server-boundary","severity":"critical","description":"The \"use client\" directive was removed but the component still calls useState and useEffect, which only work in Client Components. Next.js will fail the build with \"useState only works in a Client Component\". Either restore the \"use client\" directive (and remove the async/await fetch from the body) or split the file into a server page that fetches initial data and a client child for the polling effect."}]}
{"pr_id":"synthetic-016","language":"go","difficulty":"hard","context":"Memoize tenant config across requests","diff":"diff --git a/internal/config/tenant.go b/internal/config/tenant.go\nindex 4a5b6c7..8d9e0f1 100644\n--- a/internal/config/tenant.go\n+++ b/internal/config/tenant.go\n@@ -16,9 +16,16 @@ type Cache struct {\n     entries map[string]*Tenant\n }\n \n-func (c *Cache) Load(id string) (*Tenant, error) {\n-    c.mu.Lock()\n-    defer c.mu.Unlock()\n+func (c *Cache) Load(id string) (*Tenant, error) {\n+    c.mu.RLock()\n+    if t, ok := c.entries[id]; ok {\n+        c.mu.RUnlock()\n+        return t, nil\n+    }\n+    c.mu.RUnlock()\n+\n+    c.mu.Lock()\n+    defer c.mu.Unlock()\n     if t, ok := c.entries[id]; ok {\n         return t, nil\n     }\n@@ -27,5 +34,6 @@ func (c *Cache) Load(id string) (*Tenant, error) {\n         return nil, err\n     }\n     c.entries[id] = t\n+    go c.warmRelated(t)\n     return t, nil\n }","bugs":[{"file":"internal/config/tenant.go","line_start":37,"line_end":37,"category":"race-condition","severity":"high","description":"warmRelated is launched as a goroutine while c.mu is still held by the deferred Unlock; if warmRelated re-enters the cache (via Load or any method that takes c.mu) the goroutine blocks on the same write lock until the parent returns, but more critically warmRelated reads t (and likely c.entries) outside the lock and may observe writes from concurrent callers without synchronization. Either pass a copy of t to warmRelated or take the lock inside warmRelated; spawning the goroutine before releasing the parent's lock encodes a fragile ordering."}]}
{"pr_id":"synthetic-017","language":"python","difficulty":"medium","context":"Skip notifications for muted users","diff":"diff --git a/notifications/dispatcher.py b/notifications/dispatcher.py\nindex 5b6c7d8..9e0f1a2 100644\n--- a/notifications/dispatcher.py\n+++ b/notifications/dispatcher.py\n@@ -20,8 +20,12 @@ class Dispatcher:\n         for user in recipients:\n             if not user.email_verified:\n                 continue\n-            self._send(user, payload)\n+            if user.muted_until and user.muted_until > now():\n+                self._send(user, payload)\n+                continue\n+            self._enqueue(user, payload)\n \n     def _send(self, user, payload):\n         self.transport.send(user.email, payload)","bugs":[{"file":"notifications/dispatcher.py","line_start":23,"line_end":25,"category":"inverted-condition","severity":"high","description":"The branch sends immediately when the user is currently muted (muted_until in the future) and only enqueues when the user is not muted, which is the opposite of the intended behaviour stated by the PR title. Invert the condition: `if user.muted_until and user.muted_until > now(): continue` and call _enqueue (or _send) only for non-muted users."}]}
{"pr_id":"synthetic-018","language":"typescript","difficulty":"medium","context":"Refresh signed URLs in parallel","diff":"diff --git a/src/storage/refresh.ts b/src/storage/refresh.ts\nindex 6c7d8e9..0a1b2c3 100644\n--- a/src/storage/refresh.ts\n+++ b/src/storage/refresh.ts\n@@ -8,9 +8,12 @@ export class UrlRefresher {\n     constructor(private signer: Signer) {}\n \n     async refresh(items: Asset[]): Promise<Asset[]> {\n-        const results: Asset[] = [];\n-        for (const item of items) {\n-            results.push({ ...item, url: await this.signer.sign(item.key) });\n-        }\n-        return results;\n+        return Promise.all(\n+            items.map((item) => {\n+                const url = this.signer.sign(item.key);\n+                return { ...item, url };\n+            }),\n+        );\n     }\n }","bugs":[{"file":"src/storage/refresh.ts","line_start":13,"line_end":15,"category":"missing-await","severity":"high","description":"this.signer.sign(item.key) returns a Promise<string>, but the new map callback assigns it directly to url without awaiting, so each Asset.url ends up holding a Promise object rather than a signed URL string. The fix is to make the callback async (or return the promise) and await/Promise.all the resolved values; the type system would have caught this if Asset.url were typed as string."}]}
{"pr_id":"synthetic-019","language":"java","difficulty":"easy","context":"Compare incoming token to allowlist","diff":"diff --git a/src/main/java/com/acme/auth/TokenGate.java b/src/main/java/com/acme/auth/TokenGate.java\nindex 7d8e9f0..1a2b3c4 100644\n--- a/src/main/java/com/acme/auth/TokenGate.java\n+++ b/src/main/java/com/acme/auth/TokenGate.java\n@@ -14,7 +14,7 @@ public class TokenGate {\n     }\n \n     public boolean allow(String token) {\n-        return allowlist.contains(token);\n+        return token == \"super-admin\" || allowlist.contains(token);\n     }\n \n     public int size() {","bugs":[{"file":"src/main/java/com/acme/auth/TokenGate.java","line_start":17,"line_end":17,"category":"wrong-operator","severity":"high","description":"== compares String references in Java, not contents. Unless the incoming token happens to be the interned literal, the comparison is always false; if a caller ever does pass the literal \"super-admin\" the check unexpectedly succeeds, making the bypass behaviour dependent on string interning. Use \"super-admin\".equals(token) (and reconsider whether a hardcoded super-admin token belongs here at all)."}]}
{"pr_id":"synthetic-020","language":"python","difficulty":"hard","context":"Throttle outbound webhooks per tenant","diff":"diff --git a/services/webhooks/dispatcher.py b/services/webhooks/dispatcher.py\nindex 8e9f0a1..2b3c4d5 100644\n--- a/services/webhooks/dispatcher.py\n+++ b/services/webhooks/dispatcher.py\n@@ -12,11 +12,17 @@ class Dispatcher:\n         self.client = client\n         self.limiter = limiter\n \n-    async def dispatch(self, tenant_id: str, event: dict) -> None:\n-        await self.limiter.acquire(tenant_id)\n-        try:\n-            await self.client.post(event[\"url\"], json=event[\"payload\"])\n-        finally:\n-            await self.limiter.release(tenant_id)\n+    async def dispatch(self, tenant_id: str, event: dict) -> None:\n+        wait = self.limiter.wait_seconds(tenant_id)\n+        if wait > 0:\n+            time.sleep(wait)\n+        try:\n+            await self.client.post(event[\"url\"], json=event[\"payload\"])\n+        except httpx.HTTPError as e:\n+            log.warning(\"webhook failed: %s\", e)\n+        finally:\n+            self.limiter.consume(tenant_id)","bugs":[{"file":"services/webhooks/dispatcher.py","line_start":17,"line_end":18,"category":"blocking-in-async","severity":"high","description":"time.sleep blocks the entire asyncio event loop for the throttle duration; under contention every concurrent dispatch in the same loop stalls together, completely defeating the per-tenant rate limiter. Use await asyncio.sleep(wait) so other tasks can make progress while this one waits."}]}
{"pr_id":"synthetic-021","language":"typescript","difficulty":"medium","context":"Wire Sentry DSN in browser bundle","diff":"diff --git a/src/lib/observability.ts b/src/lib/observability.ts\nindex 9f0a1b2..3c4d5e6 100644\n--- a/src/lib/observability.ts\n+++ b/src/lib/observability.ts\n@@ -1,11 +1,15 @@\n import * as Sentry from \"@sentry/browser\";\n \n+const DSN = import.meta.env.SENTRY_DSN;\n+\n export function initObservability() {\n-    if (!import.meta.env.PROD) return;\n+    if (!DSN) {\n+        console.warn(\"Sentry DSN missing, skipping init\");\n+        return;\n+    }\n     Sentry.init({\n-        dsn: import.meta.env.VITE_SENTRY_DSN,\n-        tracesSampleRate: 0.1,\n+        dsn: DSN,\n+        tracesSampleRate: 0.1,\n         environment: import.meta.env.MODE,\n     });\n }","bugs":[{"file":"src/lib/observability.ts","line_start":3,"line_end":3,"category":"vite-env-prefix","severity":"high","description":"Vite only exposes environment variables prefixed with VITE_ to client code; SENTRY_DSN without the prefix is replaced with undefined at build time, so DSN is always falsy and Sentry is never initialised in production. Either keep VITE_SENTRY_DSN as the previous code used or rename the env var to start with VITE_."}]}
{"pr_id":"synthetic-022","language":"javascript","difficulty":"medium","context":"Serve uploaded report attachments","diff":"diff --git a/src/routes/reports.js b/src/routes/reports.js\nindex 0a1b2c3..4d5e6f7 100644\n--- a/src/routes/reports.js\n+++ b/src/routes/reports.js\n@@ -8,6 +8,12 @@ router.get(\"/reports\", async (req, res) => {\n   res.json(await reports.list());\n });\n \n+router.get(\"/reports/:id/download\", async (req, res) => {\n+  const name = req.query.file;\n+  const full = path.join(REPORTS_DIR, name);\n+  res.sendFile(full);\n+});\n+\n router.post(\"/reports\", requireAuth, async (req, res) => {\n   const r = await reports.create(req.body, req.user);\n   res.status(201).json(r);","bugs":[{"file":"src/routes/reports.js","line_start":12,"line_end":13,"category":"path-traversal","severity":"critical","description":"req.query.file is concatenated into a filesystem path with path.join, which does not strip leading ../ segments. A request like ?file=../../etc/passwd resolves outside REPORTS_DIR and sendFile happily serves it. Validate that the resolved path stays inside REPORTS_DIR (e.g. path.resolve and a startsWith check) or require an opaque report id and look the filename up server-side."}]}
{"pr_id":"synthetic-023","language":"python","difficulty":"medium","context":"Add admin endpoint to update feature flags","diff":"diff --git a/admin/flags.py b/admin/flags.py\nindex 1b2c3d4..5e6f7a8 100644\n--- a/admin/flags.py\n+++ b/admin/flags.py\n@@ -10,6 +10,13 @@ def list_flags():\n     return jsonify([f.to_dict() for f in Flag.query.all()])\n \n \n+@bp.route(\"/admin/flags\", methods=[\"POST\"])\n+@require_admin\n+def upsert_flag():\n+    payload = request.get_json()\n+    flag = Flag.upsert(name=payload[\"name\"], enabled=payload[\"enabled\"], rollout=payload.get(\"rollout\", 0))\n+    return jsonify(flag.to_dict()), 201\n+\n @bp.route(\"/admin/flags/<name>\", methods=[\"DELETE\"])\n @require_admin\n def delete_flag(name):","bugs":[{"file":"admin/flags.py","line_start":13,"line_end":17,"category":"missing-validation","severity":"high","description":"The endpoint reads `name`, `enabled`, and `rollout` from the JSON body without validating types or presence. A missing `name` raises KeyError (returned as a 500), a non-bool `enabled` is persisted verbatim, and `rollout` is unbounded so callers can write 9999 silently. Validate the payload (Marshmallow/pydantic) before calling Flag.upsert."}]}
{"pr_id":"synthetic-024","language":"typescript","difficulty":"clean","context":"Extract magic timeout into named constant","diff":"diff --git a/src/lib/http.ts b/src/lib/http.ts\nindex 2c3d4e5..6f7a8b9 100644\n--- a/src/lib/http.ts\n+++ b/src/lib/http.ts\n@@ -3,9 +3,11 @@ import { logger } from \"./logger\";\n \n type FetchOptions = RequestInit & { timeoutMs?: number };\n \n+const DEFAULT_TIMEOUT_MS = 10_000;\n+\n export async function safeFetch(url: string, options: FetchOptions = {}): Promise<Response> {\n     const controller = new AbortController();\n-    const timer = setTimeout(() => controller.abort(), options.timeoutMs ?? 10_000);\n+    const timer = setTimeout(() => controller.abort(), options.timeoutMs ?? DEFAULT_TIMEOUT_MS);\n     try {\n         return await fetch(url, { ...options, signal: controller.signal });\n     } finally {","bugs":[]}
{"pr_id":"synthetic-025","language":"go","difficulty":"easy","context":"Persist event after publish","diff":"diff --git a/internal/events/publisher.go b/internal/events/publisher.go\nindex 3d4e5f6..7a8b9c0 100644\n--- a/internal/events/publisher.go\n+++ b/internal/events/publisher.go\n@@ -14,8 +14,10 @@ func (p *Publisher) Publish(ctx context.Context, e Event) error {\n     if err := p.bus.Send(ctx, e); err != nil {\n         return err\n     }\n-    if err := p.store.Save(ctx, e); err != nil {\n-        return err\n-    }\n+    _ = p.store.Save(ctx, e)\n     return nil\n }","bugs":[{"file":"internal/events/publisher.go","line_start":17,"line_end":17,"category":"swallowed-exception","severity":"medium","description":"The error returned by p.store.Save is now discarded with `_ =`, so a failed persistence step (disk full, schema mismatch, transient DB outage) goes unnoticed and the event is acknowledged as published. Either propagate the error like the previous version did or, if the store write is intentionally best-effort, log it explicitly with the event id."}]}
{"pr_id":"synthetic-026","language":"python","difficulty":"medium","context":"Refresh OAuth tokens before expiry","diff":"diff --git a/auth/oauth.py b/auth/oauth.py\nindex 4e5f6a7..8b9c0d1 100644\n--- a/auth/oauth.py\n+++ b/auth/oauth.py\n@@ -22,10 +22,14 @@ class TokenManager:\n         return token\n \n     def refresh_if_needed(self, token: Token) -> Token:\n-        if token.expires_at - now() > timedelta(minutes=5):\n-            return token\n-        return self._refresh(token)\n+        try:\n+            if token.expires_at - now() > timedelta(minutes=5):\n+                return token\n+            return self._refresh(token)\n+        except Exception:\n+            pass\n+        return token\n \n     def _refresh(self, token: Token) -> Token:\n         resp = self.client.post(\"/oauth/token\", data={\"refresh_token\": token.refresh_token})","bugs":[{"file":"auth/oauth.py","line_start":25,"line_end":29,"category":"swallowed-exception","severity":"high","description":"A bare `except Exception: pass` swallows every refresh failure (network errors, invalid_grant, server-side revocation) and returns the stale, soon-to-expire token, which downstream calls will use until they fail with 401. Either log and re-raise, or catch only the specific transient errors that warrant returning the existing token."}]}
{"pr_id":"synthetic-027","language":"typescript","difficulty":"medium","context":"Compute total invoice amount with tax","diff":"diff --git a/src/billing/totals.ts b/src/billing/totals.ts\nindex 5f6a7b8..9c0d1e2 100644\n--- a/src/billing/totals.ts\n+++ b/src/billing/totals.ts\n@@ -7,10 +7,14 @@ export function totalCents(invoice: Invoice): number {\n     for (const line of invoice.lines) {\n         subtotal += line.unitCents * line.quantity;\n     }\n-    return subtotal + computeTax(subtotal, invoice.region);\n+    if (invoice.taxExempt) {\n+        return subtotal;\n+    }\n+    if (invoice.region === \"EU\") {\n+        return subtotal + computeTax(subtotal, invoice.region);\n+    }\n }\n \n function computeTax(subtotal: number, region: string): number {\n     return Math.round(subtotal * RATES[region]);\n }","bugs":[{"file":"src/billing/totals.ts","line_start":10,"line_end":16,"category":"wrong-return-type","severity":"high","description":"For non-EU, non-tax-exempt invoices the function falls off the end and returns undefined, even though the signature promises number. Callers that add this to other amounts will get NaN; the original branchless `return subtotal + computeTax(...)` was correct for all regions and the EU-only restriction looks unintentional. Add an unconditional fallback return."}]}
{"pr_id":"synthetic-028","language":"javascript","difficulty":"easy","context":"Wire Mapbox token for production","diff":"diff --git a/src/map/init.js b/src/map/init.js\nindex 6a7b8c9..0d1e2f3 100644\n--- a/src/map/init.js\n+++ b/src/map/init.js\n@@ -1,7 +1,9 @@\n import mapboxgl from \"mapbox-gl\";\n \n+const MAPBOX_TOKEN = \"pk.eyJ1IjoiYWNtZS1tYXBzIiwiYSI6ImNrZHk3MmEyMzAwM3oycXM4M2Y4M2pwMnUifQ.QwerAsdfZxcvBnmJklMnB\";\n+\n export function createMap(container) {\n-    mapboxgl.accessToken = process.env.MAPBOX_TOKEN;\n+    mapboxgl.accessToken = MAPBOX_TOKEN;\n     return new mapboxgl.Map({\n         container,\n         style: \"mapbox://styles/mapbox/streets-v12\",","bugs":[{"file":"src/map/init.js","line_start":3,"line_end":3,"category":"hardcoded-secret","severity":"high","description":"The Mapbox access token is hardcoded into the bundle; even though Mapbox tokens are public-facing, embedding them this way removes the ability to rotate per-environment, leaks the production token to every fork of the repo, and makes scoped tokens (referrer-restricted vs unrestricted) impossible to enforce. Read it from a build-time env var instead."}]}
{"pr_id":"synthetic-029","language":"python","difficulty":"hard","context":"Tighten signature of permission check","diff":"diff --git a/auth/permissions.py b/auth/permissions.py\nindex 7b8c9d0..1e2f3a4 100644\n--- a/auth/permissions.py\n+++ b/auth/permissions.py\n@@ -8,8 +8,11 @@ from .models import User, Resource\n \n \n-def can_access(user: User, resource: Resource, action: str = \"read\") -> bool:\n-    if user.is_superuser:\n+def can_access(user: User, resource: Resource, action: str) -> bool:\n+    if user is None:\n+        return False\n+    if user.is_superuser:\n         return True\n     return _check_acl(user, resource, action)\n \ndiff --git a/views/documents.py b/views/documents.py\nindex 8c9d0e1..2f3a4b5 100644\n--- a/views/documents.py\n+++ b/views/documents.py\n@@ -18,7 +18,7 @@ def view_document(request, doc_id):\n     doc = get_object_or_404(Document, pk=doc_id)\n     if not can_access(request.user, doc):\n         raise PermissionDenied()\n     return render(request, \"documents/view.html\", {\"doc\": doc})","bugs":[{"file":"auth/permissions.py","line_start":11,"line_end":11,"category":"breaking-api-change","severity":"high","description":"Removing the default value of `action` (\"read\") makes it a required positional argument. The unchanged caller in views/documents.py at line 20 still calls can_access(request.user, doc) without an action and now raises TypeError on every document view. Either restore the default or update all call sites in the same PR."}]}
{"pr_id":"synthetic-030","language":"typescript","difficulty":"hard","context":"Debounce search input via local state","diff":"diff --git a/src/components/SearchBox.tsx b/src/components/SearchBox.tsx\nindex 9d0e1f2..3a4b5c6 100644\n--- a/src/components/SearchBox.tsx\n+++ b/src/components/SearchBox.tsx\n@@ -10,11 +10,16 @@ export function SearchBox({ onResults }: Props) {\n     const [query, setQuery] = useState(\"\");\n     const [results, setResults] = useState<Hit[]>([]);\n \n-    useEffect(() => {\n-        const id = setTimeout(() => search(query).then(setResults), 250);\n-        return () => clearTimeout(id);\n-    }, [query]);\n+    function handleChange(e: React.ChangeEvent<HTMLInputElement>) {\n+        setQuery(e.target.value);\n+        setTimeout(() => {\n+            search(query).then((hits) => {\n+                setResults(hits);\n+                onResults(hits);\n+            });\n+        }, 250);\n+    }\n \n-    return <input value={query} onChange={(e) => setQuery(e.target.value)} />;\n+    return <input value={query} onChange={handleChange} />;\n }","bugs":[{"file":"src/components/SearchBox.tsx","line_start":13,"line_end":20,"category":"race-condition","severity":"high","description":"Two issues compound: setQuery is asynchronous, so the `query` captured by the setTimeout closure is the previous value, not e.target.value; and successive keystrokes spawn independent timers whose responses can resolve out of order, overwriting newer results with older ones. Either capture e.target.value into a local before the timeout and pass it to search, plus track an incrementing request id to drop stale responses, or restore the useEffect+cleanup pattern which cancels the prior timer."}]}
{"pr_id":"synthetic-031","language":"java","difficulty":"medium","context":"Render order summary with line items","diff":"diff --git a/src/main/java/com/acme/orders/SummaryService.java b/src/main/java/com/acme/orders/SummaryService.java\nindex 0e1f2a3..4b5c6d7 100644\n--- a/src/main/java/com/acme/orders/SummaryService.java\n+++ b/src/main/java/com/acme/orders/SummaryService.java\n@@ -18,9 +18,14 @@ public class SummaryService {\n         this.repo = repo;\n     }\n \n-    public OrderSummary summarize(long orderId) {\n-        Order order = repo.findById(orderId).orElseThrow();\n-        return OrderSummary.from(order);\n+    public List<OrderSummary> summarizeAll(List<Long> orderIds) {\n+        List<OrderSummary> out = new ArrayList<>();\n+        for (Long id : orderIds) {\n+            Order order = repo.findById(id).orElseThrow();\n+            order.getLineItems().forEach(LineItem::getProduct);\n+            out.add(OrderSummary.from(order));\n+        }\n+        return out;\n     }\n }","bugs":[{"file":"src/main/java/com/acme/orders/SummaryService.java","line_start":21,"line_end":27,"category":"n-plus-one","severity":"medium","description":"Each iteration calls repo.findById and then triggers Hibernate lazy loading of line items and their products, producing 1 + N + N*M queries for a list of N orders. Use a single fetch-join query (`@Query(\"select o from Order o join fetch o.lineItems li join fetch li.product where o.id in :ids\")`) and iterate the returned list."}]}
{"pr_id":"synthetic-032","language":"python","difficulty":"easy","context":"Log password reset attempts","diff":"diff --git a/auth/password_reset.py b/auth/password_reset.py\nindex 1f2a3b4..5c6d7e8 100644\n--- a/auth/password_reset.py\n+++ b/auth/password_reset.py\n@@ -14,7 +14,8 @@ def reset_password(email: str, new_password: str) -> bool:\n     user = User.find_by_email(email)\n     if not user:\n         return False\n+    log.info(\"reset_password: email=%s new=%s\", email, new_password)\n     user.set_password(new_password)\n     user.save()\n     return True","bugs":[{"file":"auth/password_reset.py","line_start":17,"line_end":17,"category":"pii-logging","severity":"critical","description":"The new log line writes the plaintext new password (and the email) to the application log, where it is replicated to log shippers, retained for weeks, and visible to any engineer with log access. Drop the `new=%s` field entirely; if you need to confirm the action, log only the user id and a hash-prefix, never the password."}]}
{"pr_id":"synthetic-033","language":"typescript","difficulty":"medium","context":"Show owner avatar next to repo name","diff":"diff --git a/src/components/RepoRow.tsx b/src/components/RepoRow.tsx\nindex 2a3b4c5..6d7e8f9 100644\n--- a/src/components/RepoRow.tsx\n+++ b/src/components/RepoRow.tsx\n@@ -8,9 +8,12 @@ interface Props {\n }\n \n export function RepoRow({ repo }: Props) {\n+    const owner = repo.owner;\n     return (\n         <li className=\"repo-row\">\n+            <img src={owner.avatarUrl} alt={owner.login} className=\"avatar\" />\n             <span className=\"name\">{repo.name}</span>\n+            <span className=\"owner\">{owner.login}</span>\n         </li>\n     );\n }","bugs":[{"file":"src/components/RepoRow.tsx","line_start":11,"line_end":15,"category":"null-deref","severity":"medium","description":"Repo.owner is typed as Owner | null in the schema (forks of deleted accounts have null owner); accessing owner.avatarUrl and owner.login throws when owner is null. Either narrow with `if (!owner) return null;` or render a placeholder avatar/name."}]}
{"pr_id":"synthetic-034","language":"rust","difficulty":"easy","context":"Build feature mask from flag set","diff":"diff --git a/src/features/mask.rs b/src/features/mask.rs\nindex 3b4c5d6..7e8f9a0 100644\n--- a/src/features/mask.rs\n+++ b/src/features/mask.rs\n@@ -8,7 +8,7 @@ pub fn mask(flags: &Flags) -> u32 {\n     let mut m = 0u32;\n     for (bit, enabled) in flags.iter() {\n-        if *enabled {\n+        if *enabled && bit > 0 {\n             m |= 1 << bit;\n         }\n     }\n@@ -16,5 +16,5 @@ pub fn mask(flags: &Flags) -> u32 {\n }\n \n pub fn has(mask: u32, bit: u32) -> bool {\n-    mask & (1 << bit) != 0\n+    mask && (1 << bit) != 0\n }","bugs":[{"file":"src/features/mask.rs","line_start":19,"line_end":19,"category":"wrong-operator","severity":"high","description":"The bitwise AND operator `&` was changed to logical `&&`, which the Rust compiler will reject because mask is u32, not bool. Even if it compiled, the intended check is bit-level masking, not boolean conjunction. Restore `mask & (1 << bit) != 0`."}]}
{"pr_id":"synthetic-035","language":"javascript","difficulty":"medium","context":"Render markdown comments inline","diff":"diff --git a/src/comments/Render.js b/src/comments/Render.js\nindex 4c5d6e7..8f9a0b1 100644\n--- a/src/comments/Render.js\n+++ b/src/comments/Render.js\n@@ -4,9 +4,14 @@ import { marked } from \"marked\";\n \n export function CommentBody({ comment }) {\n     const ref = useRef(null);\n     useEffect(() => {\n-        ref.current.textContent = comment.body;\n+        if (!ref.current) return;\n+        ref.current.innerHTML = marked.parse(comment.body);\n     }, [comment.body]);\n     return <div ref={ref} className=\"comment-body\" />;\n }","bugs":[{"file":"src/comments/Render.js","line_start":9,"line_end":9,"category":"xss","severity":"critical","description":"comment.body is user-controlled and is now passed to marked.parse and assigned to innerHTML; marked does not sanitize HTML by default, so a comment containing `<img onerror=...>` or `<script>` executes in the viewer's session. Either configure marked with a sanitizer (e.g. DOMPurify on the result) or use a sanitizing renderer; do not write untrusted HTML to innerHTML."}]}
{"pr_id":"synthetic-036","language":"python","difficulty":"medium","context":"Read API base URL from environment","diff":"diff --git a/app/config.py b/app/config.py\nindex 5d6e7f8..9a0b1c2 100644\n--- a/app/config.py\n+++ b/app/config.py\n@@ -6,8 +6,11 @@ class Settings:\n     def __init__(self):\n         self.db_url = os.environ[\"DATABASE_URL\"]\n         self.redis_url = os.environ[\"REDIS_URL\"]\n-        self.payments_api = os.environ[\"PAYMENTS_API_URL\"]\n+        self.payments_api = os.environ.get(\"PAYMENTS_API_URL\", \"http://localhost:4001\")\n         self.jwt_secret = os.environ[\"JWT_SECRET\"]\n+\n+    def __repr__(self) -> str:\n+        return f\"Settings(db={self.db_url!r}, payments={self.payments_api!r})\"","bugs":[{"file":"app/config.py","line_start":9,"line_end":9,"category":"hardcoded-fallback","severity":"high","description":"PAYMENTS_API_URL was previously required (raising KeyError at startup if unset); the new default of http://localhost:4001 means that in any environment where the variable is missing, payment requests silently target a local dev port — possibly nothing, possibly an unrelated service. Keep the os.environ[...] fail-fast or raise a clearer error if the variable is missing."}]}
{"pr_id":"synthetic-037","language":"typescript","difficulty":"hard","context":"Persist order then send confirmation email","diff":"diff --git a/src/orders/checkout.ts b/src/orders/checkout.ts\nindex 6e7f8a9..0b1c2d3 100644\n--- a/src/orders/checkout.ts\n+++ b/src/orders/checkout.ts\n@@ -14,11 +14,15 @@ export class Checkout {\n     async submit(cart: Cart, user: User): Promise<Order> {\n         const order = await this.orders.create(cart, user);\n-        await this.payments.charge(order, cart.paymentMethodId);\n-        await this.mailer.sendConfirmation(user, order);\n-        return order;\n+        const charged = this.payments.charge(order, cart.paymentMethodId);\n+        const mailed = this.mailer.sendConfirmation(user, order);\n+        await Promise.all([charged, mailed]);\n+        return order;\n     }\n \n     async cancel(orderId: string): Promise<void> {\n         const order = await this.orders.find(orderId);\n         await this.payments.refund(order);\n     }\n }","bugs":[{"file":"src/orders/checkout.ts","line_start":17,"line_end":20,"category":"race-condition","severity":"high","description":"The previous code awaited the charge before sending the confirmation; the new code starts both at once with Promise.all. The mailer can deliver a \"payment received\" email to the customer even when the charge ultimately rejects (insufficient funds, fraud block), and on charge failure Promise.all still rejects after the mail has been sent. Keep the sequential ordering: only mail after the charge resolves successfully."}]}
{"pr_id":"synthetic-038","language":"go","difficulty":"clean","context":"Group related types in their own file","diff":"diff --git a/internal/billing/models.go b/internal/billing/models.go\nindex 7f8a9b0..1c2d3e4 100644\n--- a/internal/billing/models.go\n+++ b/internal/billing/models.go\n@@ -1,15 +1,9 @@\n package billing\n \n-import \"time\"\n-\n type Invoice struct {\n     ID        string\n     AmountUSD int64\n-    DueAt     time.Time\n }\n \n-type LineItem struct {\n-    SKU      string\n-    Quantity int\n-}\n+type Customer struct{ ID string }\ndiff --git a/internal/billing/invoice.go b/internal/billing/invoice.go\nnew file mode 100644\nindex 0000000..2d3e4f5\n--- /dev/null\n+++ b/internal/billing/invoice.go\n@@ -0,0 +1,11 @@\n+package billing\n+\n+import \"time\"\n+\n+type LineItem struct {\n+    SKU      string\n+    Quantity int\n+}\n+\n+// re-export DueAt on Invoice via accessor in invoice_methods.go\n+var _ = time.Now","bugs":[]}
{"pr_id":"synthetic-039","language":"python","difficulty":"medium","context":"Allow downloading exported reports","diff":"diff --git a/exports/views.py b/exports/views.py\nindex 8a9b0c1..2d3e4f5 100644\n--- a/exports/views.py\n+++ b/exports/views.py\n@@ -10,8 +10,13 @@ EXPORT_DIR = Path(\"/var/data/exports\")\n \n \n @login_required\n-def download_export(request, export_id: int):\n-    export = get_object_or_404(Export, pk=export_id, owner=request.user)\n-    return FileResponse(open(export.path, \"rb\"))\n+def download_export(request):\n+    name = request.GET.get(\"name\", \"\")\n+    path = EXPORT_DIR / name\n+    if not path.exists():\n+        raise Http404\n+    return FileResponse(open(path, \"rb\"))","bugs":[{"file":"exports/views.py","line_start":13,"line_end":17,"category":"path-traversal","severity":"critical","description":"`name` comes straight from the query string and is joined to EXPORT_DIR without validation; pathlib does not strip ../, so `?name=../../etc/passwd` resolves outside the directory and FileResponse serves it. Resolve the path and assert it is a child of EXPORT_DIR (or, better, look exports up by id and the authenticated owner as the previous version did)."}]}
{"pr_id":"synthetic-040","language":"typescript","difficulty":"medium","context":"Hide archived projects from sidebar","diff":"diff --git a/src/sidebar/ProjectList.tsx b/src/sidebar/ProjectList.tsx\nindex 9b0c1d2..3e4f5a6 100644\n--- a/src/sidebar/ProjectList.tsx\n+++ b/src/sidebar/ProjectList.tsx\n@@ -8,9 +8,12 @@ interface Props {\n }\n \n export function ProjectList({ projects }: Props) {\n-    const visible = projects.filter((p) => !p.archived);\n+    const visible = projects.filter((p) => p.archived);\n     return (\n         <ul>\n             {visible.map((p) => (\n                 <ProjectLink key={p.id} project={p} />\n             ))}\n         </ul>\n     );\n }","bugs":[{"file":"src/sidebar/ProjectList.tsx","line_start":11,"line_end":11,"category":"inverted-condition","severity":"high","description":"The filter predicate was changed from `!p.archived` to `p.archived`, so the sidebar now shows only archived projects and hides the active ones. Restore the negation or rename to make intent obvious (e.g., `nonArchived`)."}]}
{"pr_id":"synthetic-041","language":"java","difficulty":"hard","context":"Add Optional return for tenant lookup","diff":"diff --git a/src/main/java/com/acme/tenant/TenantResolver.java b/src/main/java/com/acme/tenant/TenantResolver.java\nindex 0c1d2e3..4f5a6b7 100644\n--- a/src/main/java/com/acme/tenant/TenantResolver.java\n+++ b/src/main/java/com/acme/tenant/TenantResolver.java\n@@ -16,9 +16,12 @@ public class TenantResolver {\n         this.repo = repo;\n     }\n \n-    public Tenant resolve(String host) {\n-        return repo.findByHost(host).orElseThrow(() -> new TenantNotFound(host));\n-    }\n+    public Optional<Tenant> resolve(String host) {\n+        return repo.findByHost(host);\n+    }\n+\n+    public String tenantSlug(String host) {\n+        return resolve(host).get().getSlug();\n+    }\n }","bugs":[{"file":"src/main/java/com/acme/tenant/TenantResolver.java","line_start":24,"line_end":24,"category":"null-deref","severity":"high","description":"resolve() now returns Optional<Tenant> and tenantSlug calls .get() unconditionally; for any unknown host the new helper throws NoSuchElementException with no context, replacing the previous TenantNotFound that carried the host. Either keep the throwing variant (`orElseThrow(() -> new TenantNotFound(host))`) or propagate the Optional through tenantSlug."}]}
{"pr_id":"synthetic-042","language":"python","difficulty":"hard","context":"Increment usage counter in idempotent webhook","diff":"diff --git a/billing/usage.py b/billing/usage.py\nindex 1d2e3f4..5a6b7c8 100644\n--- a/billing/usage.py\n+++ b/billing/usage.py\n@@ -14,12 +14,17 @@ class UsageRecorder:\n         self.cache = cache\n \n     def record(self, account_id: str, units: int, idempotency_key: str) -> None:\n-        with self.db.transaction():\n-            seen = self.db.fetchval(\"SELECT 1 FROM usage_keys WHERE key = %s\", (idempotency_key,))\n-            if seen:\n-                return\n-            self.db.execute(\"INSERT INTO usage_keys (key) VALUES (%s)\", (idempotency_key,))\n-            self.db.execute(\"UPDATE accounts SET used = used + %s WHERE id = %s\", (units, account_id))\n+        seen = self.cache.get(f\"idem:{idempotency_key}\")\n+        if seen:\n+            return\n+        self.cache.set(f\"idem:{idempotency_key}\", \"1\", ex=86400)\n+        with self.db.transaction():\n+            self.db.execute(\"UPDATE accounts SET used = used + %s WHERE id = %s\", (units, account_id))","bugs":[{"file":"billing/usage.py","line_start":17,"line_end":22,"category":"race-condition","severity":"critical","description":"The previous version kept the idempotency check and the increment inside a single DB transaction, so two concurrent webhooks with the same key serialized on the unique INSERT into usage_keys. The new version stores the key in a cache that is non-transactional and disjoint from the DB write: two concurrent calls can both see seen=None, both write the cache, and both increment `used`, double-counting the usage. Use atomic SET NX on the cache (and even then prefer a DB unique constraint) so only one caller proceeds; also be aware that a transient cache means after eviction the same key can be replayed."}]}
{"pr_id":"synthetic-043","language":"typescript","difficulty":"medium","context":"Render release notes from CMS","diff":"diff --git a/src/components/ReleaseNotes.tsx b/src/components/ReleaseNotes.tsx\nindex 2e3f4a5..6b7c8d9 100644\n--- a/src/components/ReleaseNotes.tsx\n+++ b/src/components/ReleaseNotes.tsx\n@@ -6,10 +6,12 @@ interface Props {\n     release: Release;\n }\n \n export function ReleaseNotes({ release }: Props) {\n     return (\n         <article>\n             <h2>{release.title}</h2>\n-            <p>{release.summary}</p>\n+            <div dangerouslySetInnerHTML={{ __html: release.summary }} />\n         </article>\n     );\n }","bugs":[{"file":"src/components/ReleaseNotes.tsx","line_start":14,"line_end":14,"category":"xss","severity":"high","description":"release.summary is editable from the CMS by any authenticated content editor and is now injected via dangerouslySetInnerHTML, so any reviewer who later sees the page executes arbitrary HTML/JS authored by the editor. If rich content is required, sanitize via DOMPurify or restrict CMS input to a vetted markdown subset and render with a sanitizing parser."}]}
{"pr_id":"synthetic-044","language":"javascript","difficulty":"medium","context":"Resolve assignee name on task card","diff":"diff --git a/src/tasks/Card.js b/src/tasks/Card.js\nindex 3f4a5b6..7c8d9e0 100644\n--- a/src/tasks/Card.js\n+++ b/src/tasks/Card.js\n@@ -7,10 +7,13 @@ export function TaskCard({ task, members }) {\n     const assignee = members.find((m) => m.id === task.assigneeId);\n     return (\n         <div className=\"task-card\">\n             <h3>{task.title}</h3>\n-            <span className=\"assignee\">{task.assigneeId ? assignee.name : \"Unassigned\"}</span>\n+            <span className=\"assignee\">\n+                {assignee.name}\n+            </span>\n         </div>\n     );\n }","bugs":[{"file":"src/tasks/Card.js","line_start":11,"line_end":13,"category":"null-deref","severity":"high","description":"members.find may return undefined when the task has no assignee or when the cached members list does not include the assignee (deleted user, lazy-loaded org members). Accessing .name throws \"Cannot read properties of undefined\". Restore the conditional or default to a fallback string: `{assignee?.name ?? \"Unassigned\"}`."}]}
{"pr_id":"synthetic-045","language":"python","difficulty":"medium","context":"Filter alerts by severity and acknowledgement","diff":"diff --git a/alerts/filters.py b/alerts/filters.py\nindex 4a5b6c7..8d9e0f1 100644\n--- a/alerts/filters.py\n+++ b/alerts/filters.py\n@@ -6,8 +6,12 @@ def needs_attention(alert) -> bool:\n     return alert.severity in {\"high\", \"critical\"} and not alert.acknowledged\n \n \n def visible(alerts, *, include_low: bool = False):\n-    return [a for a in alerts if a.severity != \"low\" or include_low and a.tenant_visible]\n+    out = []\n+    for a in alerts:\n+        if a.severity != \"low\" or include_low and a.tenant_visible:\n+            out.append(a)\n+    return out","bugs":[{"file":"alerts/filters.py","line_start":12,"line_end":12,"category":"wrong-operator","severity":"medium","description":"Operator precedence makes `a.severity != \"low\" or include_low and a.tenant_visible` parse as `(a.severity != \"low\") or (include_low and a.tenant_visible)`, which always includes non-low alerts and only filters within the low-severity branch by tenant visibility — almost certainly not the intent. Add explicit parentheses: `a.severity != \"low\" or (include_low and a.tenant_visible)` is the same expression and should be reviewed for whether tenant visibility should also gate non-low alerts."}]}
{"pr_id":"synthetic-046","language":"typescript","difficulty":"easy","context":"Wire up signing key for internal webhook","diff":"diff --git a/src/integrations/internal.ts b/src/integrations/internal.ts\nindex 5b6c7d8..9e0f1a2 100644\n--- a/src/integrations/internal.ts\n+++ b/src/integrations/internal.ts\n@@ -1,8 +1,9 @@\n import { createHmac } from \"node:crypto\";\n \n+const INTERNAL_KEY = \"k_3a8f7d92ce4b1106aabd9f8e2c6d0817\";\n+\n export function signPayload(body: string): string {\n-    const key = process.env.INTERNAL_WEBHOOK_KEY ?? \"\";\n-    return createHmac(\"sha256\", key).update(body).digest(\"hex\");\n+    return createHmac(\"sha256\", INTERNAL_KEY).update(body).digest(\"hex\");\n }","bugs":[{"file":"src/integrations/internal.ts","line_start":3,"line_end":3,"category":"hardcoded-secret","severity":"critical","description":"The HMAC signing key is now embedded in source. Anyone with read access to the repo (or the published package) can forge signed payloads, bypassing the verification on the receiving end. Read the key from process.env.INTERNAL_WEBHOOK_KEY and fail loudly if it is unset rather than substituting a hardcoded constant."}]}
{"pr_id":"synthetic-047","language":"go","difficulty":"hard","context":"Return typed error from policy lookup","diff":"diff --git a/internal/policy/lookup.go b/internal/policy/lookup.go\nindex 6c7d8e9..0a1b2c3 100644\n--- a/internal/policy/lookup.go\n+++ b/internal/policy/lookup.go\n@@ -10,15 +10,21 @@ type Result struct {\n     Policy *Policy\n }\n \n-func Lookup(name string) (*Policy, error) {\n+func Lookup(name string) (*Policy, error) {\n     for _, src := range sources {\n         p, err := src.Get(name)\n-        if err == nil && p != nil {\n+        if err != nil {\n+            continue\n+        }\n+        if p != nil {\n             return p, nil\n         }\n     }\n     return nil, ErrNotFound\n }\n \n+var typedNil *Policy\n+\n+func MustLookup(name string) *Policy {\n+    p, err := Lookup(name)\n+    if err != nil {\n+        return typedNil\n+    }\n+    return p\n+}","bugs":[{"file":"internal/policy/lookup.go","line_start":29,"line_end":31,"category":"null-deref","severity":"high","description":"MustLookup returns a (*Policy)(nil) wrapped in the named typedNil variable on the error path, but its signature returns *Policy directly, so callers will dereference the result without nil-check (the helper is named Must precisely to discourage that). The rename to MustLookup also obscures the failure mode: it should panic on error like the Go convention, not silently return a typed nil that NPEs in unrelated code paths."}]}
{"pr_id":"synthetic-048","language":"python","difficulty":"clean","context":"Bump httpx pin to fix HTTP/2 regression","diff":"diff --git a/requirements.txt b/requirements.txt\nindex 7d8e9f0..1a2b3c4 100644\n--- a/requirements.txt\n+++ b/requirements.txt\n@@ -8,7 +8,7 @@ celery==5.4.0\n django==5.0.6\n djangorestframework==3.15.2\n flask==3.0.3\n-httpx==0.27.0\n+httpx==0.27.2\n psycopg[binary]==3.2.1\n pydantic==2.8.2\n redis==5.0.7","bugs":[]}
{"pr_id":"synthetic-049","language":"typescript","difficulty":"medium","context":"Auto-save draft as the user types","diff":"diff --git a/src/editor/AutoSave.tsx b/src/editor/AutoSave.tsx\nindex 8e9f0a1..2b3c4d5 100644\n--- a/src/editor/AutoSave.tsx\n+++ b/src/editor/AutoSave.tsx\n@@ -10,11 +10,15 @@ export function AutoSave({ docId }: { docId: string }) {\n     const [body, setBody] = useState(\"\");\n     const [saving, setSaving] = useState(false);\n \n-    useEffect(() => {\n-        const id = setInterval(() => save(docId, body), 5000);\n-        return () => clearInterval(id);\n-    }, [docId, body]);\n+    useEffect(() => {\n+        const id = setInterval(() => {\n+            setSaving(true);\n+            save(docId, body).finally(() => setSaving(false));\n+        }, 5000);\n+        return () => clearInterval(id);\n+    }, [docId]);\n \n     return <textarea value={body} onChange={(e) => setBody(e.target.value)} />;\n }","bugs":[{"file":"src/editor/AutoSave.tsx","line_start":17,"line_end":17,"category":"useeffect-deps","severity":"high","description":"`body` was removed from the dependency array, so the closure captured by setInterval keeps the empty initial value forever; every five seconds the component saves an empty body, overwriting the user's work. Either keep `body` in the deps (the original code did) or read it from a ref so the interval sees the latest value without resubscribing."}]}
{"pr_id":"synthetic-050","language":"java","difficulty":"medium","context":"Treat duplicate slug as conflict","diff":"diff --git a/src/main/java/com/acme/projects/ProjectService.java b/src/main/java/com/acme/projects/ProjectService.java\nindex 9f0a1b2..3c4d5e6 100644\n--- a/src/main/java/com/acme/projects/ProjectService.java\n+++ b/src/main/java/com/acme/projects/ProjectService.java\n@@ -22,8 +22,11 @@ public class ProjectService {\n     public Project create(CreateProjectCmd cmd) {\n         String slug = slugify(cmd.name());\n         Project existing = repo.findBySlug(slug);\n-        if (existing != null) {\n+        if (existing == slug) {\n             throw new SlugConflict(slug);\n         }\n         return repo.save(new Project(cmd.name(), slug));\n     }\n }","bugs":[{"file":"src/main/java/com/acme/projects/ProjectService.java","line_start":25,"line_end":25,"category":"wrong-operator","severity":"high","description":"`existing == slug` compares a Project reference to a String, which is always false (and would not even compile if the types had reference equality at runtime — here it compiles only because == on heterogeneous Object types compiles to a constant false comparison). The conflict guard never fires and duplicate slugs slip through to repo.save where a unique-key violation surfaces as a generic 500. Restore `existing != null`."}]}
{"pr_id":"synthetic-051","language":"python","difficulty":"medium","context":"Fan out reminder emails to active users","diff":"diff --git a/jobs/reminders.py b/jobs/reminders.py\nindex 0a1b2c3..4d5e6f7 100644\n--- a/jobs/reminders.py\n+++ b/jobs/reminders.py\n@@ -10,9 +10,13 @@ async def send_reminders(template: str) -> int:\n     users = await db.fetch(\"SELECT id, email FROM users WHERE active = TRUE\")\n     sent = 0\n     for user in users:\n-        await mailer.send(user[\"email\"], template)\n-        sent += 1\n+        try:\n+            requests.post(MAILER_URL, json={\"to\": user[\"email\"], \"template\": template}, timeout=5)\n+            sent += 1\n+        except requests.RequestException:\n+            log.exception(\"send failed for %s\", user[\"id\"])\n     return sent","bugs":[{"file":"jobs/reminders.py","line_start":14,"line_end":14,"category":"blocking-in-async","severity":"high","description":"requests.post is a synchronous blocking call inside an async function; for every user the event loop stalls for the full HTTP round-trip, serializing what should be concurrent fan-out and stalling unrelated tasks scheduled on the same loop. Replace with httpx.AsyncClient (or aiohttp) and `await client.post(...)`, ideally with asyncio.gather for parallelism."}]}
{"pr_id":"synthetic-052","language":"typescript","difficulty":"hard","context":"Tighten signature of permission middleware","diff":"diff --git a/src/middleware/requireRole.ts b/src/middleware/requireRole.ts\nindex 1b2c3d4..5e6f7a8 100644\n--- a/src/middleware/requireRole.ts\n+++ b/src/middleware/requireRole.ts\n@@ -3,11 +3,11 @@ import type { Request, Response, NextFunction } from \"express\";\n type Role = \"admin\" | \"member\" | \"viewer\";\n \n-export function requireRole(role: Role = \"member\") {\n+export function requireRole(role: Role) {\n     return (req: Request, res: Response, next: NextFunction) => {\n         const user = (req as any).user;\n         if (!user) return res.status(401).end();\n-        if (user.role !== role && user.role !== \"admin\") return res.status(403).end();\n+        if (user.role !== role) return res.status(403).end();\n         next();\n     };\n }","bugs":[{"file":"src/middleware/requireRole.ts","line_start":6,"line_end":6,"category":"breaking-api-change","severity":"high","description":"Two issues: removing the default value silently breaks all existing routes that called requireRole() without arguments (TypeScript will flag them but only at compile time; routes registered via dynamic factories may slip through). Additionally, line 10 dropped the `|| user.role === \"admin\"` escape hatch, so admins are now denied access to non-admin-restricted routes — a serious regression. Restore the admin-bypass and either keep the default or migrate all call sites in the same PR."},{"file":"src/middleware/requireRole.ts","line_start":10,"line_end":10,"category":"incorrect-error-handling","severity":"high","description":"The admin override (`|| user.role === \"admin\"`) was removed, so users with role admin no longer satisfy a requireRole(\"member\") guard. Restore the disjunction so admin remains a superset of all other roles, or implement a proper role-hierarchy lookup."}]}
{"pr_id":"synthetic-053","language":"javascript","difficulty":"medium","context":"Accept feedback submissions","diff":"diff --git a/src/routes/feedback.js b/src/routes/feedback.js\nindex 2c3d4e5..6f7a8b9 100644\n--- a/src/routes/feedback.js\n+++ b/src/routes/feedback.js\n@@ -8,6 +8,12 @@ router.get(\"/feedback\", requireAuth, async (req, res) => {\n     res.json(await feedback.list());\n });\n \n+router.post(\"/feedback\", async (req, res) => {\n+    const item = req.body;\n+    const saved = await feedback.create(item);\n+    res.status(201).json(saved);\n+});\n+\n router.delete(\"/feedback/:id\", requireAdmin, async (req, res) => {\n     await feedback.remove(req.params.id);\n     res.status(204).end();","bugs":[{"file":"src/routes/feedback.js","line_start":11,"line_end":15,"category":"missing-validation","severity":"high","description":"The POST route writes req.body verbatim into feedback.create with no schema validation, no size limit, and no auth (the GET above requires auth, this one does not). An attacker can store arbitrary JSON shapes — including extra fields the ORM may persist — and flood the feedback table. Add a schema validator (e.g. Joi/Zod) plus a rate limit and decide whether unauthenticated submissions are intended."}]}
{"pr_id":"synthetic-054","language":"python","difficulty":"medium","context":"Compute headline metric for dashboard","diff":"diff --git a/dashboard/metrics.py b/dashboard/metrics.py\nindex 3d4e5f6..7a8b9c0 100644\n--- a/dashboard/metrics.py\n+++ b/dashboard/metrics.py\n@@ -10,9 +10,12 @@ def headline(report: dict) -> float:\n     conv = report.get(\"conversions\")\n     visits = report.get(\"visits\")\n-    if not visits:\n-        return 0.0\n-    return conv / visits\n+    return round(conv / visits, 4)\n \n \n def headline_pct(report: dict) -> str:","bugs":[{"file":"dashboard/metrics.py","line_start":13,"line_end":13,"category":"null-deref","severity":"high","description":"`conv` and `visits` come from dict.get and may be None or 0; the new code dropped the guard so reports without `visits` raise TypeError (None / int) or ZeroDivisionError. Restore the `if not visits: return 0.0` check (or use `visits or 0` with an explicit early return)."}]}
{"pr_id":"synthetic-055","language":"rust","difficulty":"hard","context":"Stream incoming events into per-shard queues","diff":"diff --git a/src/ingest/router.rs b/src/ingest/router.rs\nindex 4e5f6a7..8b9c0d1 100644\n--- a/src/ingest/router.rs\n+++ b/src/ingest/router.rs\n@@ -22,12 +22,17 @@ impl Router {\n         let shard = (event.key_hash() % self.shards.len() as u64) as usize;\n-        let queue = self.shards[shard].lock().expect(\"poisoned\");\n-        queue.push(event);\n+        let mut queue = self.shards[shard].lock().expect(\"poisoned\");\n+        if queue.len() >= MAX_QUEUE {\n+            self.metrics.dropped.inc();\n+            return Err(IngestError::Backpressure);\n+        }\n+        queue.push(event);\n+        self.notify.notify_one();\n+        std::mem::drop(queue);\n+        self.metrics.enqueued.inc();\n         Ok(())\n     }\n }","bugs":[{"file":"src/ingest/router.rs","line_start":26,"line_end":31,"category":"resource-leak","severity":"medium","description":"On the backpressure path the early `return Err(IngestError::Backpressure)` happens while `queue` (a MutexGuard) is still alive; it is dropped at scope exit, which is correct. However, on the success path the explicit `std::mem::drop(queue)` is followed by `self.metrics.enqueued.inc()`, which is fine — but `self.notify.notify_one()` is called *while still holding the lock*, which can cause a notified consumer to wake and immediately block again on the same mutex. Move notify_one to after the drop, or rely on the natural drop at end of scope. Also reconsider whether the metrics increment should happen before or after the lock is released to keep the critical section minimal."}]}
{"pr_id":"synthetic-056","language":"typescript","difficulty":"medium","context":"Validate webhook subscription payloads","diff":"diff --git a/src/webhooks/subscribe.ts b/src/webhooks/subscribe.ts\nindex 5f6a7b8..9c0d1e2 100644\n--- a/src/webhooks/subscribe.ts\n+++ b/src/webhooks/subscribe.ts\n@@ -10,10 +10,15 @@ const schema = z.object({\n });\n \n export async function subscribe(req: Request, res: Response) {\n-    const parsed = schema.safeParse(req.body);\n-    if (!parsed.success) {\n-        return res.status(400).json({ error: parsed.error.flatten() });\n-    }\n-    const sub = await store.create(parsed.data);\n+    try {\n+        const sub = await store.create(req.body);\n+        return res.status(201).json(sub);\n+    } catch (e: any) {\n+        return res.status(400).json({ error: e.message });\n+    }\n     return res.status(201).json(sub);\n }","bugs":[{"file":"src/webhooks/subscribe.ts","line_start":13,"line_end":18,"category":"missing-validation","severity":"high","description":"The Zod safeParse step that previously enforced the request schema was replaced by a generic try/catch, so req.body is now passed unvalidated into store.create; downstream code may persist arbitrary fields, and untrusted callers learn the database error text via the response. Reinstate `schema.safeParse(req.body)` and surface the structured error."}]}
{"pr_id":"synthetic-057","language":"python","difficulty":"easy","context":"Skip orders without a tracking code","diff":"diff --git a/shipments/notify.py b/shipments/notify.py\nindex 6a7b8c9..0d1e2f3 100644\n--- a/shipments/notify.py\n+++ b/shipments/notify.py\n@@ -8,7 +8,7 @@ def notify_shipped(orders):\n     for order in orders:\n-        if order.tracking_code is None:\n+        if order.tracking_code is \"\":\n             continue\n         mailer.send(order.customer_email, \"shipped\", code=order.tracking_code)","bugs":[{"file":"shipments/notify.py","line_start":11,"line_end":11,"category":"wrong-operator","severity":"medium","description":"`is \"\"` performs identity comparison against an empty-string literal, which CPython only happens to evaluate truthy for interned literals — Python 3.8+ raises a SyntaxWarning specifically for this. The original `is None` was correct; if both None and empty string should be skipped, use `if not order.tracking_code:` (which catches None, empty, and other falsy values)."}]}
{"pr_id":"synthetic-058","language":"typescript","difficulty":"clean","context":"Add nominal type alias for tenant id","diff":"diff --git a/src/types/tenant.ts b/src/types/tenant.ts\nindex 7b8c9d0..1e2f3a4 100644\n--- a/src/types/tenant.ts\n+++ b/src/types/tenant.ts\n@@ -1,4 +1,7 @@\n-export type TenantId = string;\n+declare const tenantIdBrand: unique symbol;\n+export type TenantId = string & { readonly [tenantIdBrand]: void };\n+\n+export const asTenantId = (s: string): TenantId => s as TenantId;\n \n export interface Tenant {\n     id: TenantId;","bugs":[]}
{"pr_id":"synthetic-059","language":"java","difficulty":"clean","context":"Rename test helper for readability","diff":"diff --git a/src/test/java/com/acme/users/UserFixtures.java b/src/test/java/com/acme/users/UserFixtures.java\nindex 8c9d0e1..2f3a4b5 100644\n--- a/src/test/java/com/acme/users/UserFixtures.java\n+++ b/src/test/java/com/acme/users/UserFixtures.java\n@@ -10,11 +10,11 @@ public final class UserFixtures {\n \n     private UserFixtures() {}\n \n-    public static User make() {\n+    public static User aRandomUser() {\n         return new User(UUID.randomUUID().toString(), \"user@example.com\");\n     }\n \n-    public static User makeWithEmail(String email) {\n+    public static User aUserWithEmail(String email) {\n         return new User(UUID.randomUUID().toString(), email);\n     }\n }","bugs":[]}
{"pr_id":"synthetic-060","language":"python","difficulty":"medium","context":"Retry transient export failures","diff":"diff --git a/exports/runner.py b/exports/runner.py\nindex 9d0e1f2..3a4b5c6 100644\n--- a/exports/runner.py\n+++ b/exports/runner.py\n@@ -14,11 +14,15 @@ def run_export(export_id: int) -> None:\n     export = Export.objects.get(pk=export_id)\n     for attempt in range(3):\n         try:\n             do_export(export)\n             return\n-        except TransientError:\n-            time.sleep(2 ** attempt)\n+        except Exception:\n+            time.sleep(2 ** attempt)\n     export.mark_failed()","bugs":[{"file":"exports/runner.py","line_start":20,"line_end":21,"category":"incorrect-error-handling","severity":"high","description":"Catching the bare Exception swallows permanent failures (PermissionDenied, programming bugs, MemoryError) and retries them three times before silently calling mark_failed, hiding the real cause. Either keep TransientError as the only retried exception or whitelist a small set; let everything else propagate so the worker logs and the alerting system fires."}]}
{"pr_id":"synthetic-061","language":"typescript","difficulty":"medium","context":"Sum points across recent matches","diff":"diff --git a/src/leaderboard/score.ts b/src/leaderboard/score.ts\nindex 0e1f2a3..4b5c6d7 100644\n--- a/src/leaderboard/score.ts\n+++ b/src/leaderboard/score.ts\n@@ -8,9 +8,12 @@ export function totalPoints(matches: Match[]): number {\n     let total = 0;\n     for (const m of matches) {\n-        total += m.score;\n+        total += m.score | 0;\n+    }\n+    if (total > 2 ** 31 - 1) {\n+        total = total >>> 0;\n     }\n     return total;\n }","bugs":[{"file":"src/leaderboard/score.ts","line_start":11,"line_end":11,"category":"wrong-operator","severity":"medium","description":"`m.score | 0` coerces the score to a 32-bit signed int; any decimal points are truncated and any score above 2^31-1 wraps to negative. Match scores in this codebase can be fractional (computed from weighted multipliers), and the bitwise saturation on line 14 then mangles the running total further. Drop the bitwise ops and accumulate as a Number."}]}
{"pr_id":"synthetic-062","language":"javascript","difficulty":"hard","context":"Fan out background prefetches on page load","diff":"diff --git a/src/lib/prefetch.js b/src/lib/prefetch.js\nindex 1f2a3b4..5c6d7e8 100644\n--- a/src/lib/prefetch.js\n+++ b/src/lib/prefetch.js\n@@ -8,12 +8,18 @@ export async function prefetchAll(urls) {\n     const results = [];\n-    for (const url of urls) {\n-        results.push(await fetch(url).then((r) => r.json()));\n-    }\n+    urls.forEach((url) => {\n+        const p = fetch(url).then((r) => r.json());\n+        results.push(p);\n+    });\n+    if (results.some((r) => !r.ok)) {\n+        throw new Error(\"prefetch failed\");\n+    }\n     return results;\n }","bugs":[{"file":"src/lib/prefetch.js","line_start":13,"line_end":17,"category":"missing-await","severity":"high","description":"Two related defects: results now contains Promise objects (not resolved JSON), so the .ok check on line 15 is always false because Promises don't have an `ok` field, and callers receive an array of pending promises. Use `await Promise.all(urls.map(...))` to resolve them before inspecting and returning."}]}
{"pr_id":"synthetic-063","language":"python","difficulty":"hard","context":"Render comments with author info in template","diff":"diff --git a/blog/views.py b/blog/views.py\nindex 2a3b4c5..6d7e8f9 100644\n--- a/blog/views.py\n+++ b/blog/views.py\n@@ -16,7 +16,11 @@ def post_detail(request, slug):\n     post = get_object_or_404(Post, slug=slug)\n-    comments = post.comments.select_related(\"author\").order_by(\"created_at\")\n+    comments = (\n+        post.comments\n+        .order_by(\"created_at\")\n+        .all()\n+    )\n     return render(request, \"blog/post.html\", {\"post\": post, \"comments\": comments})","bugs":[{"file":"blog/views.py","line_start":18,"line_end":22,"category":"n-plus-one","severity":"high","description":"The previous queryset eagerly joined the `author` ForeignKey via select_related; removing it means the template's `{{ comment.author.name }}` triggers one query per comment. On a popular post this turns a 1-query render into 1 + N queries. Restore .select_related(\"author\") (and prefetch_related any reverse relations the template touches)."}]}
{"pr_id":"synthetic-064","language":"typescript","difficulty":"hard","context":"Memoize chart options for tooltip stability","diff":"diff --git a/src/components/RevenueChart.tsx b/src/components/RevenueChart.tsx\nindex 3b4c5d6..7e8f9a0 100644\n--- a/src/components/RevenueChart.tsx\n+++ b/src/components/RevenueChart.tsx\n@@ -10,11 +10,16 @@ export function RevenueChart({ series, currency }: Props) {\n     const [hover, setHover] = useState<Point | null>(null);\n \n-    const options = useMemo(\n-        () => buildOptions(series, currency),\n-        [series, currency],\n-    );\n+    const options = {\n+        ...buildOptions(series, currency),\n+        onHover: (p: Point) => setHover(p),\n+    };\n+\n+    useEffect(() => {\n+        chartRef.current?.update(options);\n+    }, [options]);\n \n     return <Chart options={options} onHover={setHover} highlight={hover} />;\n }","bugs":[{"file":"src/components/RevenueChart.tsx","line_start":13,"line_end":20,"category":"useeffect-deps","severity":"high","description":"`options` is now a fresh object every render (the useMemo was removed), so the useEffect's dependency check sees a new reference each time and runs chartRef.current.update on every render. Combined with setHover inside onHover, this creates an infinite render loop in production. Either restore the useMemo with [series, currency] (and use a ref or stable callback for setHover) or compare options by content."}]}
{"pr_id":"synthetic-065","language":"go","difficulty":"medium","context":"Decode incoming jobs and return on parse failure","diff":"diff --git a/internal/queue/consumer.go b/internal/queue/consumer.go\nindex 4c5d6e7..8f9a0b1 100644\n--- a/internal/queue/consumer.go\n+++ b/internal/queue/consumer.go\n@@ -18,11 +18,15 @@ func (c *Consumer) handle(msg amqp.Delivery) {\n     var job Job\n-    if err := json.Unmarshal(msg.Body, &job); err != nil {\n-        log.Printf(\"decode failed: %v\", err)\n-        msg.Nack(false, false)\n-        return\n-    }\n+    json.Unmarshal(msg.Body, &job)\n+    if err := c.runner.Run(job); err != nil {\n+        log.Printf(\"job failed: %v\", err)\n+        msg.Nack(false, true)\n+        return\n+    }\n+    msg.Ack(false)\n }","bugs":[{"file":"internal/queue/consumer.go","line_start":20,"line_end":20,"category":"swallowed-exception","severity":"high","description":"json.Unmarshal's error is now discarded, so a malformed message produces a zero-value Job which is then run with empty fields — possibly hitting database lookups for ID \"\" or executing the default handler. Restore the err check, log it, and Nack(false, false) so the message goes to the DLQ rather than being retried infinitely or running with garbage input."}]}
{"pr_id":"synthetic-066","language":"python","difficulty":"medium","context":"Page through search results","diff":"diff --git a/search/pagination.py b/search/pagination.py\nindex 5d6e7f8..9a0b1c2 100644\n--- a/search/pagination.py\n+++ b/search/pagination.py\n@@ -6,8 +6,11 @@ def page(items: list, page_num: int, per_page: int = 20) -> list:\n     if page_num < 1:\n         page_num = 1\n     start = (page_num - 1) * per_page\n-    end = start + per_page\n+    end = page_num * per_page - 1\n     return items[start:end]\n \n \n def total_pages(total: int, per_page: int = 20) -> int:","bugs":[{"file":"search/pagination.py","line_start":9,"line_end":9,"category":"off-by-one","severity":"medium","description":"Python slices are end-exclusive, so `items[start:end]` returns elements [start, end). The new `end = page_num * per_page - 1` returns per_page - 1 items per page instead of per_page; every page silently drops its last element. Use `end = start + per_page` or equivalently `end = page_num * per_page`."}]}
{"pr_id":"synthetic-067","language":"typescript","difficulty":"medium","context":"Fetch session token in dashboard layout","diff":"diff --git a/app/(dashboard)/layout.tsx b/app/(dashboard)/layout.tsx\nindex 6e7f8a9..0b1c2d3 100644\n--- a/app/(dashboard)/layout.tsx\n+++ b/app/(dashboard)/layout.tsx\n@@ -1,9 +1,13 @@\n \"use client\";\n+import { cookies } from \"next/headers\";\n import { Sidebar } from \"@/components/Sidebar\";\n \n export default function DashboardLayout({ children }: { children: React.ReactNode }) {\n+    const token = cookies().get(\"session\")?.value;\n     return (\n         <div className=\"dashboard\">\n-            <Sidebar />\n+            <Sidebar token={token} />\n             {children}\n         </div>\n     );","bugs":[{"file":"app/(dashboard)/layout.tsx","line_start":5,"line_end":5,"category":"client-server-boundary","severity":"critical","description":"`next/headers`' `cookies()` is a server-only API; importing it from a `\"use client\"` file fails the Next.js build with \"You're importing a component that needs `next/headers`. That only works in a Server Component\". Either drop the `\"use client\"` directive (and split out any client-only sub-components) or read the cookie via `document.cookie`/a useSession hook on the client."}]}
{"pr_id":"synthetic-068","language":"javascript","difficulty":"clean","context":"Group imports by source","diff":"diff --git a/src/components/InvoiceTable.js b/src/components/InvoiceTable.js\nindex 7f8a9b0..1c2d3e4 100644\n--- a/src/components/InvoiceTable.js\n+++ b/src/components/InvoiceTable.js\n@@ -1,8 +1,11 @@\n+import React from \"react\";\n+import classNames from \"classnames\";\n+\n+import { useInvoices } from \"../hooks/useInvoices\";\n+import { Money } from \"./Money\";\n+import { StatusPill } from \"./StatusPill\";\n+\n import \"./InvoiceTable.css\";\n-import { Money } from \"./Money\";\n-import { useInvoices } from \"../hooks/useInvoices\";\n-import { StatusPill } from \"./StatusPill\";\n-import classNames from \"classnames\";\n-import React from \"react\";\n \n export function InvoiceTable() {\n     const invoices = useInvoices();","bugs":[]}
{"pr_id":"synthetic-069","language":"python","difficulty":"medium","context":"Drop deprecated locale parameter","diff":"diff --git a/notifications/email.py b/notifications/email.py\nindex 8a9b0c1..2d3e4f5 100644\n--- a/notifications/email.py\n+++ b/notifications/email.py\n@@ -10,9 +10,9 @@ class EmailService:\n     def __init__(self, transport):\n         self.transport = transport\n \n-    def send(self, to: str, subject: str, body: str, locale: str = \"en\") -> None:\n-        rendered = render(body, locale=locale)\n+    def send(self, to: str, subject: str, body: str) -> None:\n+        rendered = render(body, locale=self.transport.default_locale)\n         self.transport.deliver(to=to, subject=subject, body=rendered)","bugs":[{"file":"notifications/email.py","line_start":13,"line_end":13,"category":"breaking-api-change","severity":"high","description":"Removing the `locale` parameter is a public API break: all existing callers passing `locale=\"de\"` etc. now raise TypeError, and silent callers that relied on the default \"en\" now get whatever transport.default_locale happens to be (often \"en_US\" or \"\"). Either keep the parameter (deprecation-warn, then remove in a follow-up) or update every call site in the same PR and bump the package version."}]}
{"pr_id":"synthetic-070","language":"typescript","difficulty":"hard","context":"Add GraphQL mutation for plan upgrade","diff":"diff --git a/src/graphql/resolvers/billing.ts b/src/graphql/resolvers/billing.ts\nindex 9b0c1d2..3e4f5a6 100644\n--- a/src/graphql/resolvers/billing.ts\n+++ b/src/graphql/resolvers/billing.ts\n@@ -10,9 +10,17 @@ export const resolvers = {\n         }),\n     },\n     Mutation: {\n+        upgradePlan: async (_: unknown, args: any, ctx: Context) => {\n+            const { accountId, plan, couponCode } = args;\n+            return ctx.billing.upgrade(accountId, plan, couponCode);\n+        },\n         cancelSubscription: async (_: unknown, { id }: { id: string }, ctx: Context) => {\n+            if (!ctx.user) throw new AuthError();\n             return ctx.billing.cancel(id, ctx.user.id);\n         },\n     },\n };","bugs":[{"file":"src/graphql/resolvers/billing.ts","line_start":13,"line_end":16,"category":"missing-validation","severity":"critical","description":"upgradePlan accepts `args: any`, runs no schema validation on plan or couponCode, and — most importantly — never checks ctx.user, so any unauthenticated client can upgrade an arbitrary accountId. Restore an auth guard (matching the cancelSubscription pattern), validate `plan` against an enum, and constrain `accountId` to the caller's tenant."}]}
{"pr_id":"synthetic-071","language":"java","difficulty":"hard","context":"Replace IOException with domain ImportError","diff":"diff --git a/src/main/java/com/acme/imports/ImportService.java b/src/main/java/com/acme/imports/ImportService.java\nindex 0c1d2e3..4f5a6b7 100644\n--- a/src/main/java/com/acme/imports/ImportService.java\n+++ b/src/main/java/com/acme/imports/ImportService.java\n@@ -16,12 +16,16 @@ public class ImportService {\n         this.parser = parser;\n     }\n \n-    public ImportResult run(Path file) throws IOException {\n+    public ImportResult run(Path file) throws ImportError {\n         try (var reader = Files.newBufferedReader(file)) {\n             return parser.parse(reader);\n+        } catch (IOException e) {\n+            throw new ImportError(\"failed to read \" + file, e);\n         }\n     }\n }","bugs":[{"file":"src/main/java/com/acme/imports/ImportService.java","line_start":19,"line_end":19,"category":"breaking-api-change","severity":"medium","description":"Changing the declared `throws` from IOException (a checked exception in Java) to ImportError affects every caller's try/catch and method signature; if ImportError extends RuntimeException callers silently lose their compile-time obligation, and if it is checked all call sites need to be updated. The diff does not include caller updates, so dependent modules will fail to compile until they are migrated. Either widen the throws to include both during a transition or update all callers in the same PR."}]}
{"pr_id":"synthetic-072","language":"python","difficulty":"medium","context":"Convert legacy timestamp to ISO string","diff":"diff --git a/lib/time/format.py b/lib/time/format.py\nindex 1d2e3f4..5a6b7c8 100644\n--- a/lib/time/format.py\n+++ b/lib/time/format.py\n@@ -6,8 +6,11 @@ from datetime import datetime, timezone\n \n \n def to_iso(ts: int | None) -> str:\n     if ts is None:\n-        return \"\"\n+        return\n     return datetime.fromtimestamp(ts, tz=timezone.utc).isoformat()","bugs":[{"file":"lib/time/format.py","line_start":10,"line_end":10,"category":"wrong-return-type","severity":"high","description":"The signature declares the function returns str, but the new `return` (no value) returns None. Callers that concatenate the result (e.g., `\"created at \" + to_iso(...)`) now hit TypeError, and serializers that strict-typecheck reject the response. Restore `return \"\"` (or update the type to `str | None` and adjust callers)."}]}
{"pr_id":"synthetic-073","language":"typescript","difficulty":"easy","context":"Bail out early when permissions are missing","diff":"diff --git a/src/auth/guards.ts b/src/auth/guards.ts\nindex 2e3f4a5..6b7c8d9 100644\n--- a/src/auth/guards.ts\n+++ b/src/auth/guards.ts\n@@ -8,7 +8,7 @@ export function requirePermission(perm: Permission) {\n     return (ctx: RequestContext) => {\n-        if (!ctx.user?.permissions.includes(perm)) {\n+        if (ctx.user?.permissions.includes(perm)) {\n             throw new ForbiddenError(perm);\n         }\n     };\n }","bugs":[{"file":"src/auth/guards.ts","line_start":11,"line_end":11,"category":"inverted-condition","severity":"critical","description":"The `!` was dropped, so the guard now throws ForbiddenError when the user *does* have the permission and lets through users who lack it — the exact opposite of the intended check. Restore the negation."}]}
{"pr_id":"synthetic-074","language":"rust","difficulty":"clean","context":"Extract shared pricing constants","diff":"diff --git a/src/pricing/mod.rs b/src/pricing/mod.rs\nindex 3f4a5b6..7c8d9e0 100644\n--- a/src/pricing/mod.rs\n+++ b/src/pricing/mod.rs\n@@ -1,10 +1,14 @@\n-pub fn monthly_cents(seats: u32) -> u64 {\n-    seats as u64 * 1500\n-}\n+pub const SEAT_PRICE_CENTS: u64 = 1500;\n+pub const MIN_SEATS: u32 = 1;\n \n-pub fn yearly_cents(seats: u32) -> u64 {\n-    seats as u64 * 1500 * 12\n-}\n+pub fn monthly_cents(seats: u32) -> u64 {\n+    seats.max(MIN_SEATS) as u64 * SEAT_PRICE_CENTS\n+}\n+\n+pub fn yearly_cents(seats: u32) -> u64 {\n+    monthly_cents(seats) * 12\n+}","bugs":[]}
{"pr_id":"synthetic-075","language":"python","difficulty":"hard","context":"Retry idempotent payment operations on network blips","diff":"diff --git a/billing/charges.py b/billing/charges.py\nindex 4a5b6c7..8d9e0f1 100644\n--- a/billing/charges.py\n+++ b/billing/charges.py\n@@ -18,12 +18,18 @@ def charge(card: Card, amount: int, idempotency_key: str) -> Charge:\n     for attempt in range(5):\n         try:\n             return gateway.charge(card, amount, idempotency_key=idempotency_key)\n-        except (ConnectionError, TimeoutError):\n-            time.sleep(2 ** attempt)\n+        except Exception as e:\n+            log.warning(\"charge attempt %d failed: %s\", attempt, e)\n+            time.sleep(2 ** attempt)\n+            idempotency_key = f\"{idempotency_key}-{attempt}\"\n     raise PaymentRetriesExhausted()","bugs":[{"file":"billing/charges.py","line_start":21,"line_end":24,"category":"incorrect-error-handling","severity":"critical","description":"Two compounding bugs: catching bare Exception now retries non-idempotent failures (CardDeclined, fraud blocks), and the idempotency_key is mutated each attempt so the gateway treats each retry as a *new* charge — risking double-charging the customer when the original request actually succeeded but the response was lost. Keep only transient network errors in the except clause and never alter the idempotency key across retries."}]}
{"pr_id":"synthetic-076","language":"typescript","difficulty":"medium","context":"Read seed file during async init","diff":"diff --git a/src/init/seed.ts b/src/init/seed.ts\nindex 5b6c7d8..9e0f1a2 100644\n--- a/src/init/seed.ts\n+++ b/src/init/seed.ts\n@@ -1,10 +1,12 @@\n-import { readFile } from \"node:fs/promises\";\n+import { readFileSync } from \"node:fs\";\n \n export async function seed(db: Database, path: string): Promise<void> {\n-    const buf = await readFile(path, \"utf8\");\n+    const buf = readFileSync(path, \"utf8\");\n     const rows = JSON.parse(buf);\n     for (const row of rows) {\n         await db.insert(\"seeds\", row);\n     }\n }","bugs":[{"file":"src/init/seed.ts","line_start":5,"line_end":5,"category":"blocking-in-async","severity":"medium","description":"readFileSync blocks the Node.js event loop for the duration of the disk I/O; if seed() is called during request handling or alongside other concurrent work, every other task on the loop stalls. Restore the asynchronous `await readFile(path, \"utf8\")` from `node:fs/promises`."}]}
{"pr_id":"synthetic-077","language":"javascript","difficulty":"hard","context":"Increment counter when add button is held","diff":"diff --git a/src/components/Counter.jsx b/src/components/Counter.jsx\nindex 6c7d8e9..0a1b2c3 100644\n--- a/src/components/Counter.jsx\n+++ b/src/components/Counter.jsx\n@@ -6,12 +6,17 @@ export function Counter() {\n     const [count, setCount] = useState(0);\n \n     function holdAdd() {\n-        const id = setInterval(() => setCount((c) => c + 1), 100);\n+        const id = setInterval(() => setCount(count + 1), 100);\n         return () => clearInterval(id);\n     }\n \n+    function resetSoon() {\n+        setTimeout(() => setCount(0), 1000);\n+        setCount(count + 5);\n+    }\n+\n     return (\n-        <button onPointerDown={holdAdd}>+</button>\n+        <><button onPointerDown={holdAdd}>+</button><button onClick={resetSoon}>reset</button></>\n     );\n }","bugs":[{"file":"src/components/Counter.jsx","line_start":9,"line_end":9,"category":"race-condition","severity":"high","description":"setCount(count + 1) closes over the `count` value at the time holdAdd was created; subsequent ticks of setInterval keep adding 1 to the *initial* count rather than the latest, so the displayed value plateaus after the first increment. The previous functional updater `setCount((c) => c + 1)` was correct because React guarantees the callback receives the latest state."},{"file":"src/components/Counter.jsx","line_start":13,"line_end":15,"category":"race-condition","severity":"medium","description":"resetSoon schedules a reset for 1s and immediately calls `setCount(count + 5)`, again with stale closure semantics. If the user clicks the button while count is non-zero, the +5 may be applied to an outdated value, then the reset arrives a second later and overwrites whatever has happened since. Use functional updaters and consider whether the timer needs to be cleared on unmount."}]}
{"pr_id":"synthetic-078","language":"python","difficulty":"medium","context":"Render rich profile bio in template","diff":"diff --git a/templates/profile.html b/templates/profile.html\nindex 7d8e9f0..1a2b3c4 100644\n--- a/templates/profile.html\n+++ b/templates/profile.html\n@@ -8,8 +8,8 @@\n     <h1>{{ user.display_name }}</h1>\n-    <p class=\"bio\">{{ user.bio }}</p>\n+    <p class=\"bio\">{{ user.bio|safe }}</p>\n     <ul class=\"links\">\n         {% for link in user.links %}\n             <li><a href=\"{{ link.url }}\">{{ link.label }}</a></li>\n         {% endfor %}\n     </ul>","bugs":[{"file":"templates/profile.html","line_start":10,"line_end":10,"category":"xss","severity":"high","description":"The `|safe` filter disables Jinja2's autoescape, but user.bio is editable by every user and is now rendered as raw HTML, allowing stored XSS via `<img onerror=...>` or `<script>`. If markdown is wanted, run user.bio through a sanitizing markdown renderer (e.g. bleach.clean(markdown(...))) before marking it safe; otherwise drop the filter."}]}
{"pr_id":"synthetic-079","language":"go","difficulty":"medium","context":"Accept new metric submissions over HTTP","diff":"diff --git a/internal/api/metrics.go b/internal/api/metrics.go\nindex 8e9f0a1..2b3c4d5 100644\n--- a/internal/api/metrics.go\n+++ b/internal/api/metrics.go\n@@ -16,6 +16,15 @@ func (h *Handler) GetMetric(w http.ResponseWriter, r *http.Request) {\n     json.NewEncoder(w).Encode(metric)\n }\n \n+func (h *Handler) SubmitMetric(w http.ResponseWriter, r *http.Request) {\n+    var m Metric\n+    if err := json.NewDecoder(r.Body).Decode(&m); err != nil {\n+        http.Error(w, err.Error(), http.StatusBadRequest)\n+        return\n+    }\n+    h.store.Save(r.Context(), m)\n+    w.WriteHeader(http.StatusCreated)\n+}\n+\n func (h *Handler) ListMetrics(w http.ResponseWriter, r *http.Request) {\n     metrics, _ := h.store.List(r.Context())\n     json.NewEncoder(w).Encode(metrics)","bugs":[{"file":"internal/api/metrics.go","line_start":19,"line_end":25,"category":"missing-validation","severity":"high","description":"The decoded Metric is forwarded to h.store.Save with no validation: missing `Name`, negative `Value`, oversized `Tags` map, or unknown fields all flow through. Combined with the silent error swallowing on the Save call (the returned error is discarded), bad data writes succeed and return 201. Validate required fields, bound `Tags`, use json.NewDecoder(...).DisallowUnknownFields(), and surface store errors as 5xx."}]}
{"pr_id":"synthetic-080","language":"typescript","difficulty":"medium","context":"Hydrate user records before serializing","diff":"diff --git a/src/api/users/index.ts b/src/api/users/index.ts\nindex 9f0a1b2..3c4d5e6 100644\n--- a/src/api/users/index.ts\n+++ b/src/api/users/index.ts\n@@ -10,9 +10,13 @@ export async function listUsers(req: Request, res: Response) {\n     const users = await db.users.findMany({ where: { tenantId: req.tenantId } });\n-    const profiles = await db.profiles.findMany({ where: { userId: { in: users.map((u) => u.id) } } });\n-    const merged = users.map((u) => ({ ...u, profile: profiles.find((p) => p.userId === u.id) }));\n+    const merged = [];\n+    for (const u of users) {\n+        const profile = await db.profiles.findFirst({ where: { userId: u.id } });\n+        merged.push({ ...u, profile });\n+    }\n     res.json(merged);\n }","bugs":[{"file":"src/api/users/index.ts","line_start":12,"line_end":15,"category":"n-plus-one","severity":"high","description":"The previous code fetched all profiles in one IN-clause query and joined in memory; the new loop issues one findFirst per user, awaiting sequentially. For a tenant with 500 users this is 500 round-trips serialized on the request. Restore the single bulk query and the in-memory map, or use Prisma's `include: { profile: true }` on the users query."}]}
{"pr_id":"synthetic-081","language":"python","difficulty":"clean","context":"Hoist URL parsing into a helper","diff":"diff --git a/lib/feeds/parser.py b/lib/feeds/parser.py\nindex 0a1b2c3..4d5e6f7 100644\n--- a/lib/feeds/parser.py\n+++ b/lib/feeds/parser.py\n@@ -8,14 +8,18 @@ from urllib.parse import urlparse\n \n+def _host_of(url: str) -> str:\n+    return urlparse(url).hostname or \"\"\n+\n+\n def collect_hosts(urls: list[str]) -> set[str]:\n     hosts = set()\n     for url in urls:\n-        host = urlparse(url).hostname\n-        if host:\n-            hosts.add(host)\n+        host = _host_of(url)\n+        if host:\n+            hosts.add(host)\n     return hosts","bugs":[]}
{"pr_id":"synthetic-082","language":"typescript","difficulty":"medium","context":"Track widget views in analytics","diff":"diff --git a/src/widgets/Embed.tsx b/src/widgets/Embed.tsx\nindex 1b2c3d4..5e6f7a8 100644\n--- a/src/widgets/Embed.tsx\n+++ b/src/widgets/Embed.tsx\n@@ -8,7 +8,9 @@ export function Embed({ id }: { id: string }) {\n     useEffect(() => {\n-        analytics.track(\"widget_view\", { id });\n+        analytics.track(\"widget_view\", { id }).catch((e) => console.error(e));\n+        analytics.flush();\n     }, [id]);\n \n     return <iframe src={`/widgets/${id}`} />;\n }","bugs":[{"file":"src/widgets/Embed.tsx","line_start":11,"line_end":11,"category":"missing-await","severity":"medium","description":"analytics.flush() returns a Promise but is fire-and-forget here; on rapid unmount/remount cycles its rejection becomes an unhandled rejection and crashes the analytics worker. Either await flush() (the effect can be made async-via-IIFE) or attach a .catch handler matching the track call."}]}
{"pr_id":"synthetic-083","language":"javascript","difficulty":"medium","context":"Parse JSON when response is OK","diff":"diff --git a/src/lib/http.js b/src/lib/http.js\nindex 2c3d4e5..6f7a8b9 100644\n--- a/src/lib/http.js\n+++ b/src/lib/http.js\n@@ -4,9 +4,11 @@ export async function getJSON(url) {\n     const res = await fetch(url);\n-    if (!res.ok) {\n-        throw new HTTPError(res.status, await res.text());\n-    }\n-    return res.json();\n+    if (res.ok) {\n+        throw new HTTPError(res.status, await res.text());\n+    }\n+    return res.json();\n }","bugs":[{"file":"src/lib/http.js","line_start":7,"line_end":7,"category":"inverted-condition","severity":"critical","description":"The `!` was removed, so the function now throws on every successful response (2xx) and tries to parse JSON only on error responses (4xx/5xx, where the body is typically a plaintext error). All callers receive an HTTPError on the happy path. Restore the negation."}]}
{"pr_id":"synthetic-084","language":"python","difficulty":"easy","context":"Stop processing when balance is zero","diff":"diff --git a/billing/drain.py b/billing/drain.py\nindex 3d4e5f6..7a8b9c0 100644\n--- a/billing/drain.py\n+++ b/billing/drain.py\n@@ -8,7 +8,7 @@ def drain(account):\n     while account.balance > 0:\n         charge = account.next_charge()\n-        if charge.amount > account.balance:\n+        if charge.amount = account.balance:\n             break\n         account.apply(charge)","bugs":[{"file":"billing/drain.py","line_start":11,"line_end":11,"category":"wrong-operator","severity":"high","description":"`if charge.amount = account.balance:` is an assignment in expression position, which is a SyntaxError in Python (unlike walrus `:=`). The function will not even import. The intended comparison is `>` (or `>=`); restore one of the comparison operators."}]}
{"pr_id":"synthetic-085","language":"typescript","difficulty":"hard","context":"Increment view count when message renders","diff":"diff --git a/src/components/Thread.tsx b/src/components/Thread.tsx\nindex 4e5f6a7..8b9c0d1 100644\n--- a/src/components/Thread.tsx\n+++ b/src/components/Thread.tsx\n@@ -10,11 +10,17 @@ export function Thread({ messages }: Props) {\n     const [views, setViews] = useState(0);\n \n     useEffect(() => {\n-        setViews((v) => v + messages.length);\n+        for (const _m of messages) {\n+            setViews(views + 1);\n+        }\n     }, [messages]);\n \n+    useEffect(() => {\n+        api.recordViews(views);\n+    }, [views]);\n+\n     return <ul>{messages.map((m) => <li key={m.id}>{m.text}</li>)}</ul>;\n }","bugs":[{"file":"src/components/Thread.tsx","line_start":13,"line_end":15,"category":"race-condition","severity":"high","description":"`setViews(views + 1)` inside the loop reads the same captured `views` value from this render, so for an array of N messages the state ends up at `views + 1`, not `views + N` — React batches the updates and the final value is whichever was set last. The previous functional update `setViews((v) => v + messages.length)` was correct. Compounding the issue, the second effect calls api.recordViews(views) on every change, so each mount records the partially-incremented count; this should batch or debounce."}]}
{"pr_id":"synthetic-086","language":"java","difficulty":"medium","context":"Stream report rows from a JDBC query","diff":"diff --git a/src/main/java/com/acme/reports/Streamer.java b/src/main/java/com/acme/reports/Streamer.java\nindex 5f6a7b8..9c0d1e2 100644\n--- a/src/main/java/com/acme/reports/Streamer.java\n+++ b/src/main/java/com/acme/reports/Streamer.java\n@@ -16,12 +16,18 @@ public class Streamer {\n     public List<Row> stream(String tenant) throws SQLException {\n         List<Row> rows = new ArrayList<>();\n-        try (var conn = ds.getConnection();\n-             var ps = conn.prepareStatement(\"SELECT id, value FROM events WHERE tenant = ?\")) {\n-            ps.setString(1, tenant);\n-            try (var rs = ps.executeQuery()) {\n-                while (rs.next()) rows.add(new Row(rs.getLong(1), rs.getString(2)));\n-            }\n-        }\n+        var conn = ds.getConnection();\n+        var ps = conn.prepareStatement(\"SELECT id, value FROM events WHERE tenant = ?\");\n+        ps.setString(1, tenant);\n+        var rs = ps.executeQuery();\n+        while (rs.next()) rows.add(new Row(rs.getLong(1), rs.getString(2)));\n         return rows;\n     }\n }","bugs":[{"file":"src/main/java/com/acme/reports/Streamer.java","line_start":18,"line_end":23,"category":"resource-leak","severity":"high","description":"The previous version used try-with-resources to guarantee Connection, PreparedStatement, and ResultSet are closed on every exit path; the rewrite removes the try-with-resources so any exception (or even the normal return) leaves the JDBC handles open. With a connection pool this exhausts the pool quickly. Restore the try-with-resources or close the resources in a finally block."}]}
{"pr_id":"synthetic-087","language":"python","difficulty":"medium","context":"Add fixture for OAuth integration tests","diff":"diff --git a/tests/integration/conftest.py b/tests/integration/conftest.py\nindex 6a7b8c9..0d1e2f3 100644\n--- a/tests/integration/conftest.py\n+++ b/tests/integration/conftest.py\n@@ -10,6 +10,12 @@ def app(db):\n     return create_app(db)\n \n \n+@pytest.fixture\n+def oauth_client():\n+    return OAuthClient(\n+        client_id=\"acme-prod\",\n+        client_secret=\"7a3c2eef-91d0-4b47-a8f2-d9c6f0a1b2e3\",\n+    )\n+\n @pytest.fixture\n def authed_client(client, user):\n     client.login(user)","bugs":[{"file":"tests/integration/conftest.py","line_start":14,"line_end":17,"category":"hardcoded-secret","severity":"high","description":"A real-looking client secret (and the production-looking client_id `acme-prod`) is committed in a test fixture. Even if this particular value is fake, the pattern teaches engineers it is acceptable to commit secrets, and a future copy-paste with a real secret will land in the repo unchallenged. Pull both values from environment variables (with a clearly synthetic default) or load them from a fixture file ignored by git."}]}
{"pr_id":"synthetic-088","language":"typescript","difficulty":"medium","context":"Notify subscribers asynchronously","diff":"diff --git a/src/events/notifier.ts b/src/events/notifier.ts\nindex 7b8c9d0..1e2f3a4 100644\n--- a/src/events/notifier.ts\n+++ b/src/events/notifier.ts\n@@ -10,11 +10,13 @@ export class Notifier {\n     constructor(private bus: Bus) {}\n \n-    async notify(event: Event): Promise<void> {\n-        for (const sub of this.subs) {\n-            await this.bus.publish(sub, event);\n-        }\n-    }\n+    async notify(event: Event): Promise<undefined> {\n+        const tasks = this.subs.map((sub) => this.bus.publish(sub, event));\n+        await Promise.all(tasks);\n+        return undefined;\n+    }\n }","bugs":[{"file":"src/events/notifier.ts","line_start":13,"line_end":17,"category":"wrong-return-type","severity":"low","description":"Changing the return type from `Promise<void>` to `Promise<undefined>` is a subtle but real public API change: callers that have typed their code against `Promise<void>` (e.g., `notify(...).then(() => ...)` with a void-returning continuation) compile fine, but consumers that accept `() => Promise<void>` no longer accept this method. There is no behavioural reason to widen to `undefined`. Revert to `Promise<void>` and drop the unnecessary `return undefined`."}]}
{"pr_id":"synthetic-089","language":"go","difficulty":"medium","context":"Track per-tenant request counts in memory","diff":"diff --git a/internal/metrics/counter.go b/internal/metrics/counter.go\nindex 8c9d0e1..2f3a4b5 100644\n--- a/internal/metrics/counter.go\n+++ b/internal/metrics/counter.go\n@@ -8,11 +8,15 @@ type Counter struct {\n     counts map[string]int64\n }\n \n-func (c *Counter) Inc(tenant string) {\n-    c.mu.Lock()\n-    defer c.mu.Unlock()\n-    c.counts[tenant]++\n-}\n+func (c *Counter) Inc(tenant string) {\n+    c.counts[tenant]++\n+}\n+\n+func (c *Counter) Snapshot() map[string]int64 {\n+    out := make(map[string]int64, len(c.counts))\n+    for k, v := range c.counts {\n+        out[k] = v\n+    }\n+    return out\n+}","bugs":[{"file":"internal/metrics/counter.go","line_start":11,"line_end":13,"category":"race-condition","severity":"high","description":"The mutex was removed from Inc, but Snapshot iterates the same map without locking either; concurrent goroutines calling Inc and Snapshot will trigger Go's runtime fatal `concurrent map read and map write` and crash the process. Restore the mutex (and acquire it for Snapshot too), or switch to sync.Map."}]}
{"pr_id":"synthetic-090","language":"python","difficulty":"medium","context":"Surface failed login reasons","diff":"diff --git a/auth/login.py b/auth/login.py\nindex 9d0e1f2..3a4b5c6 100644\n--- a/auth/login.py\n+++ b/auth/login.py\n@@ -16,8 +16,12 @@ def login(email: str, password: str) -> LoginResult:\n     user = User.find_by_email(email)\n     if user is None or not user.verify_password(password):\n-        log.info(\"login failed for %s\", email)\n+        log.warning(\n+            \"login failed: email=%s pw=%s ip=%s\",\n+            email, password, request.remote_addr,\n+        )\n         return LoginResult.failure()\n     return LoginResult.success(user)","bugs":[{"file":"auth/login.py","line_start":18,"line_end":21,"category":"pii-logging","severity":"critical","description":"The new log line writes the candidate plaintext password and the email to the application log on every failed login. Beyond the obvious credential exposure (typo'd-but-correct passwords land in logs), this is a near-certain GDPR/SOC2 finding. Drop `pw=%s`; if forensic value is needed, log a hash of the password and a coarse IP."}]}
{"pr_id":"synthetic-091","language":"typescript","difficulty":"clean","context":"Prefer const enum for Status values","diff":"diff --git a/src/types/status.ts b/src/types/status.ts\nindex 0e1f2a3..4b5c6d7 100644\n--- a/src/types/status.ts\n+++ b/src/types/status.ts\n@@ -1,6 +1,7 @@\n-export const Status = {\n-    Pending: \"pending\",\n-    Active: \"active\",\n-    Archived: \"archived\",\n-} as const;\n-export type Status = (typeof Status)[keyof typeof Status];\n+export const Status = {\n+    Pending: \"pending\",\n+    Active: \"active\",\n+    Archived: \"archived\",\n+} as const;\n+export type Status = (typeof Status)[keyof typeof Status];\n+export const STATUSES: Status[] = [Status.Pending, Status.Active, Status.Archived];","bugs":[]}
{"pr_id":"synthetic-092","language":"python","difficulty":"hard","context":"Increment session counter atomically","diff":"diff --git a/auth/sessions.py b/auth/sessions.py\nindex 1f2a3b4..5c6d7e8 100644\n--- a/auth/sessions.py\n+++ b/auth/sessions.py\n@@ -16,11 +16,16 @@ class SessionStore:\n     def issue(self, user_id: int) -> Session:\n         with self._lock:\n             count = self._counts.get(user_id, 0)\n-            if count >= self.max_per_user:\n-                raise TooManySessions()\n-            self._counts[user_id] = count + 1\n-        token = secrets.token_urlsafe(32)\n+        if count >= self.max_per_user:\n+            raise TooManySessions()\n+        token = secrets.token_urlsafe(32)\n+        with self._lock:\n+            self._counts[user_id] = self._counts.get(user_id, 0) + 1\n         self._sessions[token] = Session(user_id=user_id, token=token)\n         return token","bugs":[{"file":"auth/sessions.py","line_start":18,"line_end":24,"category":"race-condition","severity":"high","description":"The check-then-act for max_per_user was split across two lock acquisitions: thread A reads count=N-1 in the first block, releases the lock, thread B does the same, both see < max, both increment. The cap is silently exceeded under concurrency. Keep the read, the bound check, and the increment inside a single critical section as the previous code did."}]}
{"pr_id":"synthetic-093","language":"typescript","difficulty":"medium","context":"Render external profile link","diff":"diff --git a/src/components/ExternalLink.tsx b/src/components/ExternalLink.tsx\nindex 2a3b4c5..6d7e8f9 100644\n--- a/src/components/ExternalLink.tsx\n+++ b/src/components/ExternalLink.tsx\n@@ -6,8 +6,8 @@ interface Props {\n }\n \n export function ExternalLink({ user }: Props) {\n-    if (!user.website) return null;\n-    return <a href={user.website} rel=\"noopener noreferrer\" target=\"_blank\">{user.website}</a>;\n+    if (!user.website) return null;\n+    return <a href={user.website}>{user.website}</a>;\n }","bugs":[{"file":"src/components/ExternalLink.tsx","line_start":10,"line_end":10,"category":"xss","severity":"medium","description":"`user.website` is user-controlled. The previous version included `rel=\"noopener noreferrer\" target=\"_blank\"` and the URL was rendered as-is — but an attacker can supply a `javascript:` URL and the click executes script in the current origin. Validate that the href starts with http:// or https://, and restore the rel/target attributes (their removal is a separate regression: the linked page can now access window.opener)."}]}
{"pr_id":"synthetic-094","language":"java","difficulty":"medium","context":"Look up audit entries by user","diff":"diff --git a/src/main/java/com/acme/audit/AuditRepository.java b/src/main/java/com/acme/audit/AuditRepository.java\nindex 3b4c5d6..7e8f9a0 100644\n--- a/src/main/java/com/acme/audit/AuditRepository.java\n+++ b/src/main/java/com/acme/audit/AuditRepository.java\n@@ -18,9 +18,13 @@ public class AuditRepository {\n         this.jdbc = jdbc;\n     }\n \n-    public List<AuditEntry> findByUser(String userId) {\n-        return jdbc.query(\n-            \"SELECT * FROM audit WHERE user_id = ?\",\n-            new Object[]{userId}, AuditEntry::map);\n-    }\n+    public List<AuditEntry> findByUser(String userId) {\n+        String sql = \"SELECT * FROM audit WHERE user_id = '\" + userId + \"'\";\n+        var ps = jdbc.getDataSource().getConnection().prepareStatement(sql);\n+        var rs = ps.executeQuery();\n+        var out = new ArrayList<AuditEntry>();\n+        while (rs.next()) out.add(AuditEntry.map(rs, rs.getRow()));\n+        return out;\n+    }\n }","bugs":[{"file":"src/main/java/com/acme/audit/AuditRepository.java","line_start":22,"line_end":22,"category":"sql-injection","severity":"critical","description":"The previous code passed userId as a bound parameter; the rewrite concatenates it into the SQL string before constructing the PreparedStatement, defeating the whole point of using one. Any caller that lets userId be influenced by request input now has classic SQL injection. Restore parameterized binding (`prepareStatement(\"SELECT * FROM audit WHERE user_id = ?\")` and `ps.setString(1, userId)`) — and close the connection/statement/resultset with try-with-resources."}]}
{"pr_id":"synthetic-095","language":"python","difficulty":"medium","context":"Schedule cleanup task after request","diff":"diff --git a/api/uploads.py b/api/uploads.py\nindex 4c5d6e7..8f9a0b1 100644\n--- a/api/uploads.py\n+++ b/api/uploads.py\n@@ -16,9 +16,12 @@ async def upload(request: Request) -> Response:\n     asset = await store.save(await request.body())\n-    asyncio.create_task(cleanup_temp_files(asset.id))\n+    cleanup_temp_files(asset.id)\n     return Response.json({\"id\": asset.id})\n \n \n async def cleanup_temp_files(asset_id: str) -> None:\n     await asyncio.sleep(60)\n     await tmp.delete(asset_id)","bugs":[{"file":"api/uploads.py","line_start":18,"line_end":18,"category":"missing-await","severity":"high","description":"cleanup_temp_files is `async def`, so calling it without await (or asyncio.create_task) returns an unawaited coroutine that is immediately garbage-collected with a `RuntimeWarning: coroutine was never awaited`; no cleanup actually runs. Restore `asyncio.create_task(cleanup_temp_files(asset.id))` so it runs in the background."}]}
{"pr_id":"synthetic-096","language":"typescript","difficulty":"medium","context":"Decode cached entry from KV store","diff":"diff --git a/src/cache/decode.ts b/src/cache/decode.ts\nindex 5d6e7f8..9a0b1c2 100644\n--- a/src/cache/decode.ts\n+++ b/src/cache/decode.ts\n@@ -10,8 +10,11 @@ interface CacheEntry {\n }\n \n export function decode<T extends CacheEntry>(raw: string | null): T {\n-    const parsed = JSON.parse(raw ?? \"{}\") as T;\n-    return parsed;\n+    const parsed = JSON.parse(raw!);\n+    return parsed;\n }","bugs":[{"file":"src/cache/decode.ts","line_start":13,"line_end":14,"category":"null-deref","severity":"high","description":"The `?? \"{}\"` fallback was replaced with a non-null assertion `raw!`. When the cache miss returns null, JSON.parse(null) returns the value null (not an object), which is then cast to T and used by callers as if it were a CacheEntry — accessing any property throws \"Cannot read properties of null\". Restore the nullish-coalesce or branch: `if (raw === null) return defaultEntry();`."}]}
{"pr_id":"synthetic-097","language":"javascript","difficulty":"easy","context":"Fetch latest exchange rates","diff":"diff --git a/src/lib/rates.js b/src/lib/rates.js\nindex 6e7f8a9..0b1c2d3 100644\n--- a/src/lib/rates.js\n+++ b/src/lib/rates.js\n@@ -2,8 +2,8 @@ const ENDPOINT = \"https://rates.example.com/latest\";\n \n export async function latestRates() {\n     const res = await fetch(ENDPOINT);\n-    const json = await res.json();\n+    const json = res.json();\n     return json.rates;\n }","bugs":[{"file":"src/lib/rates.js","line_start":5,"line_end":5,"category":"missing-await","severity":"high","description":"res.json() returns a Promise, not the parsed body; `json.rates` therefore evaluates `Promise.rates` which is undefined, and every caller silently sees no rates. Restore the await: `const json = await res.json();`."}]}
{"pr_id":"synthetic-098","language":"python","difficulty":"medium","context":"Skip sync when source is offline","diff":"diff --git a/sync/runner.py b/sync/runner.py\nindex 7f8a9b0..1c2d3e4 100644\n--- a/sync/runner.py\n+++ b/sync/runner.py\n@@ -10,9 +10,12 @@ def sync(source) -> None:\n     status = source.health_check()\n-    if status != \"ok\":\n-        log.info(\"skipping %s: status=%s\", source.id, status)\n-        return\n+    if status == \"ok\":\n+        log.info(\"skipping %s: status=%s\", source.id, status)\n+        return\n+    log.info(\"syncing %s\", source.id)\n     for record in source.iter_records():\n         destination.write(record)","bugs":[{"file":"sync/runner.py","line_start":12,"line_end":14,"category":"inverted-condition","severity":"critical","description":"The condition was flipped from `!= \"ok\"` to `== \"ok\"`, so the runner now skips healthy sources and proceeds to iterate records on unhealthy ones — the exact opposite of the PR's stated intent and a guaranteed source of partial/garbage data downstream. Restore the original if status != \"ok\": return."}]}
{"pr_id":"synthetic-099","language":"rust","difficulty":"medium","context":"Cache compiled regexes per pattern","diff":"diff --git a/src/match/cache.rs b/src/match/cache.rs\nindex 8a9b0c1..2d3e4f5 100644\n--- a/src/match/cache.rs\n+++ b/src/match/cache.rs\n@@ -14,11 +14,15 @@ impl RegexCache {\n         Self { inner: Mutex::new(HashMap::new()) }\n     }\n \n-    pub fn get(&self, pat: &str) -> Result<Regex, regex::Error> {\n-        let mut map = self.inner.lock().unwrap();\n-        if let Some(r) = map.get(pat) {\n-            return Ok(r.clone());\n-        }\n-        let compiled = Regex::new(pat)?;\n-        map.insert(pat.to_string(), compiled.clone());\n-        Ok(compiled)\n-    }\n+    pub fn get(&self, pat: &str) -> Result<Regex, regex::Error> {\n+        {\n+            let map = self.inner.lock().unwrap();\n+            if let Some(r) = map.get(pat) {\n+                return Ok(r.clone());\n+            }\n+        }\n+        let compiled = Regex::new(pat)?;\n+        self.inner.lock().unwrap().insert(pat.to_string(), compiled.clone());\n+        Ok(compiled)\n+    }\n }","bugs":[{"file":"src/match/cache.rs","line_start":17,"line_end":26,"category":"race-condition","severity":"medium","description":"Releasing the lock between the read and the write opens a TOCTOU window: two threads can both miss in the map, both compile the same regex (Regex::new is expensive — the cache exists to avoid this), and one's insert overwrites the other. The fix is double-checked locking: re-check the map after acquiring the write lock and only insert if still absent."}]}
{"pr_id":"synthetic-100","language":"typescript","difficulty":"clean","context":"Tighten type of pagination cursor","diff":"diff --git a/src/types/pagination.ts b/src/types/pagination.ts\nindex 9b0c1d2..3e4f5a6 100644\n--- a/src/types/pagination.ts\n+++ b/src/types/pagination.ts\n@@ -1,6 +1,10 @@\n-export interface Page<T> {\n-    items: T[];\n-    cursor?: string;\n-}\n+export interface Page<T> {\n+    readonly items: ReadonlyArray<T>;\n+    readonly cursor?: string;\n+}\n+\n+export interface PageRequest {\n+    readonly cursor?: string;\n+    readonly limit?: number;\n+}","bugs":[]}