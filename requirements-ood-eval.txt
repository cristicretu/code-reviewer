# Minimal requirements for running ood_testing/run_ood_eval.py
# Assumes torch + CUDA already installed on the pod.
#
# Usage:
#   pip install -r requirements-ood-eval.txt

transformers>=4.41
peft>=0.11
accelerate>=0.31
huggingface-hub>=0.23
safetensors>=0.4
sentencepiece>=0.2
bitsandbytes>=0.43
sacrebleu>=2.4
rouge-score>=0.1.2
code-bert-score>=0.4
