% ===========================================================================
% Team Messi -- Code Reviewer
% 10 min, 2 presenters.
% Compile with EITHER engine:
%   pdflatex main.tex   (run twice)
%   xelatex  main.tex   (run twice)
% Requires the `metropolis` beamer theme and `fontawesome5` (both shipped
% with TeX Live / MacTeX `full`; with BasicTeX run `tlmgr install metropolis
% fontawesome5 pgfopts`).
% ===========================================================================
\documentclass[10pt,aspectratio=169]{beamer}

\usetheme{metropolis}
\usepackage{appendixnumberbeamer}

\usepackage{booktabs}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, fit, backgrounds, shapes.geometric}
\usepackage{xcolor}
\usepackage{fontawesome5}
\usepackage{microtype}

% --- Modern, restrained palette --------------------------------------------
\definecolor{accent}{HTML}{2E5BFF}
\definecolor{accent2}{HTML}{12B886}
\definecolor{ink}{HTML}{1F2933}
\definecolor{mute}{HTML}{8492A6}
\definecolor{soft}{HTML}{F3F4F6}

\setbeamercolor{normal text}{fg=ink}
\setbeamercolor{alerted text}{fg=accent}
\setbeamercolor{frametitle}{bg=white, fg=ink}
\setbeamercolor{progress bar}{fg=accent, bg=soft}
\setbeamercolor{title separator}{fg=accent}

% --- Title metadata --------------------------------------------------------
\title{Code Reviewer}
\subtitle{An agentic LLM that reviews pull requests on every push}
\date{}
\author{\textbf{Team Messi} \\[2pt]
  \small Cretu Cristian \;\textperiodcentered\; Cretu Luca \;\textperiodcentered\;
  Greholea Denis \;\textperiodcentered\; Gosa Bogdan \;\textperiodcentered\; Hiticas Paul}
\institute{}

% --- Reusable bits ---------------------------------------------------------
\newcommand{\chip}[1]{%
  \tikz[baseline=(c.base)]\node[draw=mute, rounded corners=3pt, inner sep=3pt,
        fill=soft, font=\scriptsize] (c) {#1};}

\newcommand{\stat}[2]{%
  \begin{tikzpicture}
    \node[font=\Large\bfseries, text=accent] (n) {#1};
    \node[below=2pt of n, font=\scriptsize, text=mute] {#2};
  \end{tikzpicture}}

% ===========================================================================
\begin{document}

% ---------- 1. Title -------------------------------------------------------
\maketitle

% ---------- 2. Introduction ------------------------------------------------
\begin{frame}{What we built}
  A \alert{GitHub Action} that runs on every pull request and posts \alert{one} batched
  review with inline comments and a verdict
  (\textsc{approve} / \textsc{request\_changes} / \textsc{comment}).
  \vspace{0.6em}

  \begin{columns}[T,onlytextwidth]
    \column{0.55\textwidth}
      \begin{itemize}
        \item Open-source, runs in any repo with 3 secrets
        \item Repo-aware: indexes the codebase before reviewing
        \item Self-hosted model -- no per-PR API cost
      \end{itemize}
    \column{0.42\textwidth}
      \chip{\faGithub\ PR opened / synchronized}\\[6pt]
      \chip{\faRobot\ Agent loop, $\leq$ 20 steps}\\[6pt]
      \chip{\faCommentDots\ 1 review, $\leq$ 10 inline comments}
  \end{columns}
\end{frame}

% ---------- 3. Problem Definition ------------------------------------------
\begin{frame}{Problem}
  \begin{columns}[T,onlytextwidth]
    \column{0.48\textwidth}
      \textbf{Manual review}
      \begin{itemize}
        \item Slow -- blocks merges, reviewers context-switch
        \item Inconsistent across reviewers and repos
        \item Doesn't scale with team size or PR rate
      \end{itemize}
    \column{0.48\textwidth}
      \textbf{Off-the-shelf LLM review}
      \begin{itemize}
        \item No project context -- hits only the diff
        \item Generic comments, hallucinated APIs
        \item Can't actually run tests, lint, or grep
      \end{itemize}
  \end{columns}
  \vspace{0.8em}
  \begin{block}{Goal}
    Reviews that are \alert{specific}, \alert{grounded in the repo}, and produced by an
    agent that can \alert{investigate} before commenting.
  \end{block}
\end{frame}

% ---------- 4. State of the Art --------------------------------------------
\begin{frame}{State of the art}
  \begin{table}
    \footnotesize
    \begin{tabular}{@{}lccc@{}}
      \toprule
       & \textbf{Repo-aware} & \textbf{Open / self-host} & \textbf{Investigates} \\
      \midrule
      GitHub Copilot Review            & partial & \ding{55} & \ding{55} \\
      CodeRabbit / Greptile            & yes     & \ding{55} & partial   \\
      Cursor / VSCode review hints     & partial & \ding{55} & \ding{55} \\
      Microsoft CodeReviewer (2022)    & \ding{55} & yes    & \ding{55} \\
      \midrule
      \textbf{Ours}                    & \alert{yes} & \alert{yes} & \alert{yes} \\
      \bottomrule
    \end{tabular}
  \end{table}
  \vspace{0.4em}
  \textbf{Microsoft CodeReviewer} is the dataset we train and evaluate on -- code diffs paired with the
  actual comment a human reviewer left on that diff in a real open-source GitHub PR. After cleaning, we
  train on $\sim$94k pairs and hold out 11.7k for evaluation.
\end{frame}

% ---------- 5. Proposed Solution -- Architecture ---------------------------
\begin{frame}{Solution -- architecture}
  \vspace{0.3em}
  \centering
  \begin{tikzpicture}[
      node distance=12mm and 11mm,
      box/.style={draw=mute, rounded corners=3pt, inner sep=5pt, font=\scriptsize,
                  minimum height=9mm, align=center, fill=soft},
      accbox/.style={box, draw=accent, fill=white, text=accent,
                     font=\scriptsize\bfseries, minimum width=22mm},
      arr/.style={-Latex, mute, thick},
      arrdash/.style={-Latex, mute, thick, dashed}]
    % Top row: linear pipeline
    \node[box] (pr)  {\faGithub\ PR opened\\or pushed};
    \node[box, right=of pr] (act) {GitHub Action\\fetches diff,\\indexes the repo};
    \node[accbox, right=of act] (agent) {\faRobot\ Agent\\loop};
    \node[box, right=of agent] (review) {\faCommentDots\ 1 batched\\review back\\to the PR};

    \draw[arr] (pr) -- (act);
    \draw[arr] (act) -- (agent);
    \draw[arr] (agent) -- (review);

    % Bottom row: what the agent uses
    \node[box, below=14mm of agent, xshift=-32mm, minimum width=36mm] (model)
      {\faMicrochip\ \textbf{Model:} Qwen3.5-9B\\SFT (QLoRA) $\rightarrow$ GRPO};
    \node[box, below=14mm of agent, minimum width=28mm] (tools)
      {\faWrench\ \textbf{17 tools}\\search / run / post};
    \node[box, below=14mm of agent, xshift=32mm, minimum width=36mm] (rag)
      {\faDatabase\ \textbf{Indexed codebase}\\Chroma + CodeRankEmbed};

    \draw[arrdash] (model.north) -- ([xshift=-7mm]agent.south);
    \draw[arrdash] (tools.north) -- (agent.south);
    \draw[arrdash] (rag.north)   -- ([xshift=7mm]agent.south);
  \end{tikzpicture}
  \vspace{0.5em}\\
  \footnotesize\textcolor{mute}{Top: pipeline. Bottom: what the agent calls into during the loop.}
\end{frame}

% ---------- 6b. Dataset ----------------------------------------------------
\begin{frame}{Dataset -- Microsoft CodeReviewer}
  \chip{Source: Zenodo records/6900648}\;\chip{\textit{Comment Generation} split}\;\chip{846 MB raw}
  \vspace{0.4em}

  Each example in the dataset is a real code diff from an open-source GitHub PR, paired
  with the actual comment a human reviewer wrote on that diff. We use the public
  \textit{Comment Generation} split to teach the model the same task: read a diff, write a
  review.
  \vspace{0.6em}

  \begin{columns}[T,onlytextwidth]
    \column{0.50\textwidth}
      \textbf{Cleaning before training}
      \begin{itemize}\small
        \item Drop comments under 10 characters (mostly noise)
        \item Drop generic acknowledgements: \textit{lgtm}, \textit{+1}, \textit{done}, \textit{nit}, \textit{ack}, \textit{fixed}
        \item Drop bare URLs and formatting-only changes
        \item Convert the dataset's \texttt{<add>}/\texttt{<del>} tags into a normal \texttt{+}/\texttt{-} line-prefix diff
      \end{itemize}
    \column{0.46\textwidth}
      \textbf{One example becomes a 3-turn chat}
      \begin{itemize}\small
        \item \textbf{system}: a fixed instruction (next slide)
        \item \textbf{user}: \texttt{Review this code change: ```diff ... ```}
        \item \textbf{assistant}: the human reviewer's actual comment from the PR
      \end{itemize}
  \end{columns}
  \vspace{0.5em}
  \footnotesize After cleaning we shuffle and split 80/10/10 (seed 42): \alert{$\sim$94k train},
  \alert{$\sim$11.7k val}, \alert{11.7k test}. The test split is held out -- every number in our
  results tables is computed on examples the model has never seen.
\end{frame}

% ---------- 6c. SFT implementation -----------------------------------------
\begin{frame}{SFT -- our setup}
  We fine-tune Qwen3.5-9B on the prepared chat-format dataset using TRL's
  \texttt{SFTTrainer} with Unsloth's kernel patches for speed. Only the LoRA adapters are
  updated; the base model stays frozen and quantised.
  \vspace{0.5em}
  \begin{columns}[T,onlytextwidth]
    \column{0.50\textwidth}
      \textbf{Training} \;\chip{TRL SFTTrainer + Unsloth}\;\chip{A100 80GB}
      \begin{itemize}\small
        \item Base model loaded in NF4 4-bit, bf16 compute, \texttt{max\_seq=2048}
        \item QLoRA adapters: rank $r{=}16$, $\alpha{=}16$, dropout 0
        \item Adapters attached to every projection layer (\texttt{q,k,v,o,gate,up,down\_proj})
        \item lr \texttt{2e-4}, cosine schedule, 10 warmup steps, AdamW 8-bit
        \item Per-device batch 32, weight decay 0.01, grad clip 1.0
        \item \alert{1 epoch} only -- more starts overfitting to the noisier reviewer comments
      \end{itemize}
      \vspace{4pt}
      \footnotesize \faBox\ Published adapter: \texttt{cristicretu/code-reviewer-lora}
    \column{0.46\textwidth}
      \begin{block}{\footnotesize System prompt sent with every training example}
        \scriptsize\itshape You are an expert code reviewer. Given a code diff,
        provide a concise, actionable review comment. Focus on bugs, security
        vulnerabilities, performance issues, and code style. Be specific:
        reference the exact code that needs changing and explain why. If the
        code looks correct, say so briefly.
      \end{block}
  \end{columns}
\end{frame}

% ---------- 6d. GRPO our setup + reward prompt -----------------------------
\begin{frame}{GRPO -- our setup \& reward}
  \begin{columns}[T,onlytextwidth]
    \column{0.48\textwidth}
      \textbf{Reward}\;\chip{Claude Haiku 4.5 judge}
      \begin{itemize}\scriptsize
        \item Scores each candidate review 1--5 on \emph{technical correctness} + \emph{specificity}
        \item Length penalty up to $-0.5$ over 100 words -- keeps comments tight
        \item 8 parallel judge workers, retry / backoff on rate-limits
      \end{itemize}
      \vspace{4pt}
      \textbf{Training} \;\chip{TRL GRPOTrainer + Unsloth}
      \begin{itemize}\scriptsize
        \item Start from the SFT adapter, same LoRA shape
        \item $G{=}4$ completions / prompt at $T{=}0.8$
        \item \texttt{max\_prompt=512}, \texttt{max\_completion=512}
        \item lr \texttt{2e-6}, batch 1, grad accum 4, 1 epoch, bf16
        \item \alert{250 prompts} (subset of SFT training data)
      \end{itemize}
      \vspace{4pt}
      \footnotesize \faBox\ \texttt{cretu-luca/code-reviewer-grpo} \;|\; \texttt{...-grpo-GGUF} (Q4\_K\_M, 5.2\,GB)
    \column{0.50\textwidth}
      \begin{block}{\footnotesize Reward prompt (verbatim, abridged)}
        \tiny\ttfamily
        You are evaluating an automated code review comment on a code diff.\\[3pt]
        \textless code-diff\textgreater~ [DIFF] ~\textless/code-diff\textgreater\\
        \textless review-comment\textgreater~ [COMMENT] ~\textless/review-comment\textgreater\\[3pt]
        Score 1--5 on technical correctness and specificity.\\
        Length, tone, confidence do not affect the score.\\[3pt]
        5 -- real issue, exact location, explains why, concrete fix.\\
        4 -- real issue but vague about location, cause, or fix.\\
        3 -- partially correct; explanation or fix incomplete.\\
        2 -- generic; could apply to any diff.\\
        1 -- factually wrong, refers to code not in the diff, or toxic.\\[3pt]
        Respond with a single integer (1--5) and nothing else.
      \end{block}
  \end{columns}
\end{frame}

% ===========================================================================
% Hand-off to presenter B
% ===========================================================================

% ---------- 7. RAG --------------------------------------------------------
\begin{frame}{RAG -- indexing the repo}
  Before the agent runs, the action walks the checked-out repo and indexes it into
  a per-repo Chroma collection. The agent's \texttt{semantic\_search} tool queries
  that collection during the review.
  \vspace{0.6em}
  \begin{columns}[T,onlytextwidth]
    \column{0.48\textwidth}
      \textbf{Ingestion}
      \begin{itemize}\small
        \item Walks the repo, skips build / cache dirs (\texttt{.git}, \texttt{node\_modules}, \texttt{.venv}, \texttt{\_\_pycache\_\_}, \texttt{dist}, \texttt{build}, \dots)
        \item Keeps \texttt{.py}, \texttt{.md}, \texttt{.ts}, \texttt{.js}, \texttt{.go}, \texttt{.java}, \texttt{.rs}
        \item Drops empty files and anything over \alert{200\,KB}
        \item \alert{Chunking}: one file = one snippet, no sub-file splitting
        \item Sent in batches of 25 to the ingest endpoint
      \end{itemize}
    \column{0.48\textwidth}
      \textbf{Embeddings \& storage}
      \begin{itemize}\small
        \item Embedder: \texttt{nomic-ai/CodeRankEmbed}, run locally via \texttt{SentenceTransformer}
        \item \texttt{chromadb.PersistentClient} on disk, one collection per repository, HNSW with cosine distance
        \item Doc IDs are a SHA-256 of \texttt{(repo, path, chunk\_index, content)}; we \texttt{upsert} so re-ingesting unchanged content is a no-op
      \end{itemize}
  \end{columns}
  \vspace{0.6em}
  \footnotesize\textbf{Retrieval:} \texttt{semantic\_search(query, k)} returns the top-$k$ chunks with
  their file path and a cosine-similarity score (computed as $1 - \text{distance}$). $k$ defaults to 10,
  capped at 20.
\end{frame}

% ---------- 7b. Agent & tools ---------------------------------------------
\begin{frame}{Agent \& tools}
  \textbf{smolagents \texttt{CodeAgent}} -- writes Python tool calls, ends with
  \texttt{final\_answer("done")}.\;
  Defaults: \alert{20 steps}, \alert{10 inline comments} (both tunable via env).
  \vspace{0.3em}
  \begin{columns}[T,onlytextwidth]
    \column{0.33\textwidth}
      \textbf{\small Retrieval}
      \begin{itemize}\scriptsize
        \item \texttt{semantic\_search}\\(CodeRankEmbed + Chroma, behind a local FastAPI service)
        \item \texttt{search\_keyword} (\texttt{grep -rn})
        \item \texttt{search\_symbol} (Python \texttt{ast}, \texttt{.py} files)
        \item \texttt{get\_file} (path + optional \texttt{line\_range})
      \end{itemize}
    \column{0.33\textwidth}
      \textbf{\small Execution}
      \begin{itemize}\scriptsize
        \item \texttt{run\_tests} (\texttt{pytest})
        \item \texttt{run\_linter} (\texttt{ruff check})
        \item \texttt{run\_typecheck} (\texttt{mypy})
        \item \texttt{run\_snippet} (\texttt{python3} subprocess, 5\,s timeout)
      \end{itemize}
    \column{0.33\textwidth}
      \textbf{\small Context}
      \begin{itemize}\scriptsize
        \item \texttt{check\_history} (\texttt{git blame})
        \item \texttt{get\_pr\_metadata} (GitHub REST: title, author, base/head, files)
        \item \texttt{get\_team\_conventions}\\(\texttt{CLAUDE.md} / \texttt{CONTRIBUTING.md} / \texttt{.cursorrules})
        \item \texttt{load\_skill} -- playbooks (\texttt{react}, \texttt{nextjs}, \texttt{vite}, \texttt{supabase}, \texttt{async-js})
      \end{itemize}
  \end{columns}
  \vspace{0.3em}
  \textbf{\small Action} \;\textcolor{mute}{\scriptsize buffered into \texttt{ReviewState}, sent as one review}
  \begin{itemize}\scriptsize
    \item \texttt{post\_comment(file, line, severity, category, suggestion)} \;|\; \texttt{propose\_patch(file, line, diff)}
    \item Verdict (\alert{first-call-wins}): \texttt{approve} \;|\; \texttt{request\_changes} \;|\; \texttt{comment\_only}
  \end{itemize}
  \vspace{0.2em}
  \footnotesize \texttt{ReviewState} dedups by $(file, line)$, enforces the budget, and POSTs one
  \texttt{/repos/.../pulls/.../reviews} at the end -- auto-downgrades \texttt{APPROVE}\,$\rightarrow$\,\texttt{COMMENT} when GitHub returns 422.
\end{frame}

% ---------- 8a. Static eval -- automatic metrics --------------------------
\begin{frame}{Static evaluation -- automatic metrics}
  \textbf{Setup} \;\chip{100 ex sampled from 11{,}772 CodeReviewer test set}\;\chip{base vs SFT vs RLHF}
  \vspace{0.5em}
  \begin{columns}[T,onlytextwidth]
    \column{0.62\textwidth}
      \footnotesize
      \begin{tabular}{@{}lccc@{}}
        \toprule
         & \textbf{Base (qwen3:8b)} & \textbf{SFT} & \textbf{RLHF} \\
        \midrule
        Defect F1            & 0.001 & 0.070  & \textbf{0.072} \\
        CodeBERTScore        & 0.000 & 0.668  & \textbf{0.669} \\
        ChrF                 & 0.29  & 13.45  & \textbf{13.93} \\
        ROUGE-L              & 0.001 & 0.049  & \textbf{0.050} \\
        Coherence            & 0.009 & 0.491  & \textbf{0.507} \\
        \midrule
        Hallucination rate   & 100\% & 76\%   & 81\% \\
        Toxicity rate        & 0\%   & 0\%    & 0\% \\
        \midrule
        Avg prediction length & 3 ch & $\sim$900 ch & $\sim$900 ch \\
        \bottomrule
      \end{tabular}
    \column{0.34\textwidth}
      \textbf{Reading the table}
      \begin{itemize}\scriptsize
        \item Base outputs are \alert{essentially empty} (3-char average) -- it never learned the format
        \item SFT and RLHF produce structured, $\sim$900-char reviews
        \item RLHF nudges ahead on every overlap metric -- gap is consistent but small
        \item Hallucination is heuristic (identifier not in the diff); reviews legitimately introduce names when proposing fixes, inflating this
        \item Toxicity zero -- expected for OSS review data
      \end{itemize}
  \end{columns}
\end{frame}

% ---------- 8b. Static eval -- LLM-as-judge --------------------------------
\begin{frame}{Static evaluation -- LLM-as-judge}
  \textbf{Setup} \;\chip{Gemini 2.5 Flash}\;\chip{10 ex per model}\;\chip{1--5 on three axes}
  \vspace{0.4em}
  \begin{columns}[T,onlytextwidth]
    \column{0.52\textwidth}
      \begin{block}{\footnotesize Judge prompt (abridged)}
        \tiny\ttfamily
        You are evaluating the quality of an automated code review comment.\\[2pt]
        \#\# Code Diff~~~~~~~~~~~~ [DIFF]\\
        \#\# Review Comment~~~~ [COMMENT]\\
        \#\# Ground Truth ~~~~~~ [REFERENCE]\\[3pt]
        Score 1--5 on three dimensions:\\[2pt]
        \textbf{Accuracy} -- identifies a real issue? 1 if hallucinated, 5 if precisely identified.\\
        \textbf{Helpfulness} -- actionable suggestion? 1 if vague/generic, 5 if a clear fix.\\
        \textbf{Specificity} -- references exact code/lines? 1 if generic, 5 if pinpoints.\\[3pt]
        Respond with JSON only:~ [accuracy, helpfulness, specificity, reasoning].
      \end{block}
    \column{0.44\textwidth}
      \footnotesize
      \begin{tabular}{@{}lcc@{}}
        \toprule
         & \textbf{SFT} (n=10) & \textbf{RLHF} (n=8) \\
        \midrule
        Accuracy     & \textbf{2.40} & 1.50 \\
        Helpfulness  & \textbf{1.90} & 1.25 \\
        Specificity  & \textbf{3.70} & 2.62 \\
        \bottomrule
      \end{tabular}
      \vspace{8pt}

      \textbf{Findings}
      \begin{itemize}\scriptsize
        \item Both tuned models crush base across the board
        \item Auto metrics said RLHF was marginally ahead -- the Gemini judge \alert{disagrees and prefers SFT}
        \item Consistent with \alert{reward over-fit to Haiku}: a single-judge GRPO setup is a real risk
        \item Take-away: rotate judges next time (Haiku / Gemini / Gemma)
      \end{itemize}
  \end{columns}
\end{frame}

% ---------- 8c. Agentic eval -- setup + summary ---------------------------
\begin{frame}{Agentic evaluation -- setup \& metrics}
  \textbf{Setup} \;\chip{4-bit GGUF agent, local}\;\chip{judge: gemma-4-e4b-it}\;\chip{15 ex, max 20 steps}
  \vspace{0.5em}
  \begin{columns}[T,onlytextwidth]
    \column{0.48\textwidth}
      \textbf{Three metrics}
      \begin{itemize}\scriptsize
        \item \textbf{Task completion} (programmatic) -- 1.0 if a valid verdict was set before the step budget ran out, else 0.0
        \item \textbf{Task correctness} (LLM-judged, Gemma GEval) -- 0--1, aligned with the RLHF reward rubric
        \item \textbf{Step efficiency} (programmatic) -- decays linearly above the 10-call cap; 0.3 for verdict-with-no-investigation; 0 for no verdict
      \end{itemize}
      \vspace{6pt}
      \begin{block}{\footnotesize GEval rubric for correctness}
        \scriptsize
        Is the flagged issue real and present in the diff?\\
        Does the comment pinpoint the exact location (file, line)?\\
        Does it explain \emph{why} the code is problematic?\\
        Does it propose a concrete, actionable fix?\\
        Is it specific to this diff, not a generic observation?
      \end{block}
    \column{0.48\textwidth}
      \footnotesize
      \begin{tabular}{@{}lccc@{}}
        \toprule
        Metric & Mean & Pass rate & Threshold \\
        \midrule
        Task completion   & 0.85 & 13/15 (86.7\%) & $\geq$ 0.50 \\
        Task correctness  & 0.85 & 13/15 (86.7\%) & $\geq$ 0.50 \\
        Step efficiency   & 0.82 & 12/15 (80.0\%) & $\geq$ 0.50 \\
        \bottomrule
      \end{tabular}
      \vspace{8pt}

      \textbf{What ``step efficiency'' means}
      \begin{itemize}\scriptsize
        \item Best score: 1--2 tool calls and a valid verdict
        \item 0.3 floor for the \emph{verdict with 0 tool calls} case -- a valid verdict without any investigation
        \item 0 for runs that never reach a verdict
      \end{itemize}
  \end{columns}
\end{frame}

% ---------- 8d. Agentic eval -- distributions + analysis -------------------
\begin{frame}{Agentic evaluation -- distributions \& analysis}
  \begin{columns}[T,onlytextwidth]
    \column{0.40\textwidth}
      \footnotesize\textbf{Verdict distribution}\\[2pt]
      \begin{tabular}{@{}lcc@{}}
        \toprule
        Verdict & N & \% \\
        \midrule
        \textsc{approve}          & 10 & 66.7 \\
        \textsc{comment}          & 2  & 13.3 \\
        \textsc{request\_changes} & 1  & 6.7 \\
        \midrule
        No verdict (budget out)   & 2  & 13.3 \\
        \bottomrule
      \end{tabular}
      \vspace{8pt}

      \footnotesize\textbf{Tool calls per run}\\[2pt]
      \begin{tabular}{@{}cc@{}}
        \toprule
        Calls & Runs \\
        \midrule
        0 & 2 \\
        1 & 8 \\
        2 & 3 \\
        3 & 1 \\
        5 & 1 \\
        \bottomrule
      \end{tabular}
      \vspace{4pt}

      \scriptsize Avg 1.60 \;|\; 1.85 over completed runs \;|\; max 5
    \column{0.56\textwidth}
      \textbf{Analysis}
      \begin{itemize}\small
        \item \alert{2 budget-exhausted runs} share a pattern: the model emitted prose review text instead of Python tool calls -- smolagents has nothing executable to run
        \item Best correctness on the \textsc{comment} / \textsc{request\_changes} cases -- those force the agent to post a specific inline comment first
        \item 2 \textsc{approve}-with-0-calls runs drag the efficiency tail down (0.3 floor)
        \item Step budget is comfortable: max-observed 5 / cap 10
      \end{itemize}
  \end{columns}
\end{frame}

% ---------- 10. Demo -------------------------------------------------------
\begin{frame}[standout]
  Demo
  \vspace{6pt}\\
\end{frame}

\end{document}
