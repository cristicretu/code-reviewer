# Default target: A100 40 GB+
# For a local pipeline validation, run with --smoke-test instead of editing.

model:
  adapter: "cristicretu/code-reviewer-lora"  # HF repo or local path e.g. outputs/sft/final
  max_seq_length: 2048

lora:
  r: 16
  alpha: 16
  dropout: 0
  target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj
    - gate_proj
    - up_proj
    - down_proj

data:
  train_path: "data/processed/train.jsonl"
  max_train_samples: 500

training:
  num_train_epochs: 1
  per_device_batch_size: 1
  gradient_accumulation_steps: 4
  learning_rate: 5.0e-6
  num_generations: 2
  temperature: 0.8
  max_prompt_length: 512
  max_new_tokens: 128
  seed: 42

judge:
  model: "claude-haiku-4-5-20251001"
  sleep_between_calls: 0.25
  max_workers: 8

output:
  dir: "outputs/grpo"
  logging_steps: 10
  save_steps: 100
  save_total_limit: 2
  hub_model_id: null
  report_to: "none"

output:
  dir: "outputs/grpo"
  logging_steps: 10
  save_steps: 100
  save_total_limit: 2
  hub_model_id: null
  report_to: "none"

